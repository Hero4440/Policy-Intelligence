---
phase: 09-patient-cases-coverage-evaluation
plan: "02"
type: execute
wave: 1
depends_on:
  - "09-01"
files_modified:
  - src/storage/types.ts
  - src/storage/patient-store.ts
  - src/server/patient-routes.ts
autonomous: true
requirements:
  - PATC-04
  - PATC-05

must_haves:
  truths:
    - "Uploaded patient documents produce structured extracted facts for diagnosis, requested drug, prior therapies, prescriber type, and insurance info when present"
    - "Every extracted fact carries source-document linkage and a readable evidence snippet"
    - "Patient case detail payloads expose extracted facts alongside the originating document records"
  artifacts:
    - path: "src/storage/patient-store.ts"
      provides: "Deterministic patient fact extraction and fact persistence"
      exports:
        - addCaseDocument
        - getPatientCase
    - path: "src/server/patient-routes.ts"
      provides: "Case detail route returning facts + source documents"
      exports:
        - registerPatientRoutes
  key_links:
    - from: "src/storage/patient-store.ts"
      to: "src/mcp/fhir/extractors.ts"
      via: "FHIR bundle extraction reuse"
      pattern: "extractPatientData"
    - from: "src/storage/patient-store.ts"
      to: "case detail payload"
      via: "document upload updates extractedFacts on the stored case"
      pattern: "extractFactsFromDocumentContent"
---

<objective>
Turn uploaded patient documents into evidence-backed extracted facts that are usable by both the case-detail UI and the upcoming evaluation engine.

Purpose: Phase 9 is only valuable if the patient evidence chain is explicit and inspectable.
Output: upgraded extraction logic and case-detail payloads with fact-level evidence
</objective>

<execution_context>
@/Users/tanmaybhuskute/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tanmaybhuskute/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-patient-cases-coverage-evaluation/09-CONTEXT.md
@src/storage/patient-store.ts
@src/storage/types.ts
@src/mcp/fhir/extractors.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Strengthen deterministic extraction for supported patient document types</name>
  <files>src/storage/patient-store.ts, src/storage/types.ts</files>
  <action>
    Expand extraction coverage without introducing LLM dependence.

    Required extraction targets:
    - diagnosis
    - requested drug
    - prior therapies
    - prescriber type
    - insurance/payer info

    Required behavior:
    - keep JSON FHIR bundle support and reuse the existing FHIR extraction helpers
    - for text documents, improve heuristics so common note formats produce explicit fact records for the required targets when present
    - add or refine fact categories as needed, but keep them stable and frontend-friendly
    - every fact must include:
      - `sourceDocumentId`
      - `label`
      - `value`
      - `confidence`
      - `evidenceSnippet`
    - if a document is too sparse to extract a required field, do not invent the field; leave it absent and preserve the note excerpt as a fallback fact where useful
  </action>
  <verify>
    `npx tsc --noEmit` passes.
    Uploading a representative note with diagnosis, requested drug, prior therapy, prescriber, and payer fields produces matching extracted facts.
  </verify>
  <done>
    Patient extraction produces actionable, evidence-backed fact records instead of only generic note summaries.
  </done>
</task>

<task type="auto">
  <name>Task 2: Return evidence-ready facts from case detail responses</name>
  <files>src/storage/patient-store.ts, src/server/patient-routes.ts</files>
  <action>
    Ensure the case detail payload is sufficient for PATC-05 and for the later evaluation screen.

    Requirements:
    - `GET /api/patients/cases/:caseId` returns:
      - case metadata
      - stored document records
      - extracted facts
    - document summaries should be meaningful enough for the user to distinguish uploads
    - extracted fact evidence snippets should be readable in the UI without requiring another backend lookup
    - if a document is re-uploaded with the same source intent, facts for that document should be replaced deterministically rather than duplicated endlessly
  </action>
  <verify>
    Fetch one case after document upload and confirm the payload contains documents + extracted facts linked by `sourceDocumentId`.
  </verify>
  <done>
    PATC-04 and PATC-05 are satisfied at the backend/data-contract layer.
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- extracted facts include required Phase 9 categories when the source document contains them
- case detail payload shows facts and their source linkage together
</verification>

<success_criteria>
- Uploaded documents become inspectable structured patient evidence
- The case-detail UI can show facts next to their source-document context
- PATC-04 and PATC-05 are implemented at the backend/domain layer
</success_criteria>

<output>
After completion, create `.planning/phases/09-patient-cases-coverage-evaluation/09-02-SUMMARY.md` with:
- extraction heuristics added or refined
- fact categories and evidence fields returned
- any intentional limitations of the deterministic extractor
- verification results
</output>
