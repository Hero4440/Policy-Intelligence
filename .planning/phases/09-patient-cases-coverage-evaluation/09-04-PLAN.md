---
phase: 09-patient-cases-coverage-evaluation
plan: "04"
type: execute
wave: 3
depends_on:
  - "09-01"
  - "09-02"
  - "09-03"
files_modified:
  - src/frontend/data/patients.ts
  - src/frontend/components/patient-cases-view.tsx
  - src/frontend/components/patient-case-detail-view.tsx
  - src/frontend/App.tsx
  - src/frontend/styles.css
autonomous: false
requirements:
  - PATC-01
  - PATC-02
  - PATC-03
  - PATC-05
  - EVAL-01
  - EVAL-02
  - EVAL-03
  - EVAL-04
  - EVAL-05

must_haves:
  truths:
    - "The Patients page supports case creation, document upload, extracted-fact review, policy selection, and evaluation results in one coherent workflow"
    - "Users can trigger an evaluation from the case detail screen and inspect the saved result without leaving the Patients page"
    - "Checklist items visibly connect matched patient facts and policy evidence"
  artifacts:
    - path: "src/frontend/components/patient-case-detail-view.tsx"
      provides: "Phase 9 patient case detail and evaluation workflow"
      exports:
        - PatientCaseDetailView
    - path: "src/frontend/data/patients.ts"
      provides: "Patient case and evaluation API client helpers"
      exports:
        - fetchPatientCases
        - fetchPatientCase
        - createStoredPatientCase
        - uploadPatientDocument
  key_links:
    - from: "src/frontend/components/patient-case-detail-view.tsx"
      to: "/api/patients/cases/:caseId/evaluations"
      via: "evaluate button and saved evaluation refresh"
      pattern: "createCaseEvaluation"
    - from: "src/frontend/App.tsx"
      to: "src/frontend/components/patient-cases-view.tsx"
      via: "Patients page master-detail orchestration"
      pattern: "activePage === 'patients'"
---

<objective>
Turn the existing Patients screen into the full Phase 9 clinic workflow: intake on the left, case detail in the center, and evaluation results with linked evidence in the detail view.

Purpose: Phase 9 is only complete when clinic staff can operate the workflow end-to-end in the browser.
Output: Patients page UI covering case intake, extracted facts, evaluation, and saved results
</objective>

<execution_context>
@/Users/tanmaybhuskute/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tanmaybhuskute/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/09-patient-cases-coverage-evaluation/09-CONTEXT.md
@src/frontend/App.tsx
@src/frontend/data/patients.ts
@src/frontend/components/patient-cases-view.tsx
@src/frontend/components/patient-case-detail-view.tsx
@src/frontend/styles.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add frontend patient evaluation data helpers and app orchestration</name>
  <files>src/frontend/data/patients.ts, src/frontend/App.tsx</files>
  <action>
    Extend the frontend patient data layer and App-level state so the Patients page can drive evaluation.

    Required additions:
    - API helpers for:
      - creating an evaluation
      - listing evaluations for a case
      - fetching one evaluation if needed
    - frontend types mirroring the saved evaluation payload, including checklist item evidence linkage
    - App-level refresh logic so:
      - creating a case refreshes the list
      - uploading a document refreshes the selected case
      - running an evaluation refreshes both the selected case and its saved evaluations
  </action>
  <verify>
    `npm run frontend:build` passes.
    The Patients page loads without runtime errors with the new data helpers wired in.
  </verify>
  <done>
    The frontend can talk to the full Phase 9 patient/evaluation API surface.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build the complete patient case and evaluation UI</name>
  <files>src/frontend/components/patient-cases-view.tsx, src/frontend/components/patient-case-detail-view.tsx, src/frontend/styles.css</files>
  <action>
    Expand the Patients page into a full workflow screen.

    UI requirements:
    - case list still supports creation with:
      - patient name
      - payer
      - requested drug
      - diagnosis
    - case detail shows:
      - case metadata
      - uploaded documents
      - extracted facts with evidence snippets
      - policy selector and version selector for evaluation
      - evaluate action
      - latest saved evaluation result
      - checklist with status badges
    - each checklist item should display:
      - criterion text
      - patient fact match or missing-state label
      - source policy evidence snippet/reference

    UX requirements:
    - clear empty states for no documents, no facts, and no saved evaluation
    - prevent evaluation until minimum prerequisites exist, or fail clearly if allowed and incomplete
    - the saved evaluation must remain visible after page refresh
  </action>
  <verify>
    `npm run frontend:build` passes.
    In the browser, create a case, upload a note, run an evaluation, and confirm the result stays visible after a refresh.
  </verify>
  <done>
    The Patients page satisfies the end-user UI flow for Phase 9.
  </done>
</task>

</tasks>

<verification>
- `npm run frontend:build` passes
- patient workflow is usable end-to-end in the browser
- evaluation results remain visible after refresh because they are reloaded from the backend
</verification>

<success_criteria>
- Clinic staff can complete the Phase 9 workflow entirely from the Patients page
- Extracted facts and saved evaluations are inspectable and linked to evidence
- The frontend satisfies the user-visible portion of PATC and EVAL requirements
</success_criteria>

<output>
After completion, create `.planning/phases/09-patient-cases-coverage-evaluation/09-04-SUMMARY.md` with:
- Patients page workflow changes
- evaluation UI states added
- any UI constraints or intentionally deferred polish
- verification results
</output>
