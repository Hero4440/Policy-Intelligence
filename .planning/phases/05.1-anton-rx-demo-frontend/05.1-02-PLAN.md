---
phase: 05.1-anton-rx-demo-frontend
plan: 02
type: execute
wave: 2
depends_on: ["05.1-01"]
files_modified:
  - src/frontend/App.tsx
  - src/frontend/components/search-bar.tsx
  - src/frontend/components/coverage-overview.tsx
  - src/frontend/components/policy-detail.tsx
  - src/frontend/components/compare-panel.tsx
  - src/frontend/components/change-panel.tsx
  - src/frontend/data/policy-diff.ts
autonomous: true

must_haves:
  truths:
    - "Users can answer which plans cover a selected drug through the UI"
    - "Users can inspect prior auth criteria and evidence for a selected plan/drug pair"
    - "Users can compare policies side-by-side across payers"
    - "The UI includes an honest change-tracking panel that supports the Anton Rx track story"
  artifacts:
    - path: "src/frontend/components/compare-panel.tsx"
      provides: "Side-by-side payer comparison experience"
      contains: "Compare"
    - path: "src/frontend/components/policy-detail.tsx"
      provides: "Criteria and evidence detail view"
      contains: "evidence"
    - path: "src/frontend/components/change-panel.tsx"
      provides: "Policy change storytelling panel"
      contains: "Change"
  key_links:
    - from: "src/frontend/components/policy-detail.tsx"
      to: "data/policies/structured/*.json"
      via: "Structured criteria and evidence surface directly in the UI"
      pattern: "evidenceText|stepTherapy|diagnosisRequirements"
    - from: "src/frontend/components/change-panel.tsx"
      to: "src/frontend/data/policy-diff.ts"
      via: "Change panel uses explicit demo diff logic rather than vague text"
      pattern: "changed"
---

<objective>
Implement the Anton Rx workflows on top of the frontend shell: search, coverage lookup, criteria inspection, payer comparison, and an honest change-tracking story.

Purpose: The frontend is only useful for the hackathon if it directly answers the Anton Rx judging questions in a polished, fast-to-demo interaction flow.
Output: Searchable and comparable frontend workflows for drug coverage, PA criteria, and policy-change storytelling.
</objective>

<execution_context>
@/Users/hero4440/.claude/get-shit-done/workflows/execute-plan.md
@/Users/hero4440/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05.1-anton-rx-demo-frontend/05.1-01-PLAN.md
@data/policies/structured/policies-index.json
@data/policies/structured/uhc-adalimumab-ra.json
@data/policies/structured/aetna-upadacitinib-ra.json
@data/policies/structured/cigna-infliximab-ra.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Anton Rx query flows in the UI</name>
  <files>
    src/frontend/App.tsx
    src/frontend/components/search-bar.tsx
    src/frontend/components/coverage-overview.tsx
    src/frontend/components/policy-detail.tsx
  </files>
  <action>
Implement the primary UI flows so the demo can answer:
- Which plans cover Drug X?
- What prior auth criteria does Plan Y require for Drug Z?

Requirements:
- Search or filter by drug
- Surface payer/plan coverage states clearly
- Show criteria, evidence snippets, and source metadata in a detail panel
- Keep interaction speed high; this is a demo product surface, not a back-office CRUD app
  </action>
  <verify>
Select a drug and confirm the UI renders multiple payer/plan results plus a detailed criteria view for one selection.
  </verify>
  <done>
The frontend can answer the first two Anton Rx questions cleanly with the real normalized data.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add payer comparison and change-tracking story</name>
  <files>
    src/frontend/components/compare-panel.tsx
    src/frontend/components/change-panel.tsx
    src/frontend/data/policy-diff.ts
    src/frontend/App.tsx
  </files>
  <action>
Build two additional demo panels:

1. **Comparison panel**
- Show two or more payer policies side-by-side for the selected drug
- Highlight differences in coverage, PA requirement shape, or restrictions

2. **Change panel**
- If true quarter-over-quarter data is unavailable, build a clearly labeled demo baseline or placeholder state with explicit honesty about what is real vs simulated
- Do not fake historical claims without labels

The panel should help tell the Anton Rx story: policy coverage is fragmented and hard to compare without normalization.
  </action>
  <verify>
The UI shows a side-by-side comparison and a visible change-oriented panel for at least one selected drug scenario.
  </verify>
  <done>
The frontend covers all core Anton Rx judging flows and includes an honest change-tracking narrative.
  </done>
</task>

</tasks>

<verification>
1. Frontend runs locally without errors
2. Search/coverage query works
3. Criteria detail view works
4. Compare panel works
5. Change panel is present and clearly labeled
</verification>

<success_criteria>
- Anton Rx track questions can be demonstrated directly in the UI
- Comparison is a first-class workflow, not buried in the interface
- Change-tracking is represented honestly and clearly
</success_criteria>

<output>
After completion, create `.planning/phases/05.1-anton-rx-demo-frontend/05.1-02-SUMMARY.md`
</output>
