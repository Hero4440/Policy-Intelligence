---
phase: 05.1-anton-rx-demo-frontend
plan: 05
type: execute
wave: 5
depends_on: ["05.1-04"]
files_modified:
  - src/server/antonrx-data.ts
  - src/server/antonrx-routes.ts
  - src/server/chat.ts
  - src/frontend/App.tsx
  - src/frontend/components/ask-view.tsx
  - src/frontend/components/changes-view.tsx
  - src/frontend/components/compare-view.tsx
  - src/frontend/components/policy-list.tsx
  - src/frontend/data/antonrx.ts
  - src/frontend/styles.css
autonomous: true

must_haves:
  truths:
    - "The main Anton Rx story is now fully analyst-focused with no leftover patient-readiness framing in the primary UX"
    - "Coverage results clearly communicate provenance and confidence rather than pretending every source is equally strong"
    - "Change-watch provides useful signals about cross-plan divergence and source vintage, not just a placeholder sentence"
    - "The workspace is usable without chat, with chat acting as an accelerator over the same deterministic data"
  artifacts:
    - path: "src/server/antonrx-data.ts"
      provides: "Confidence-aware coverage/change summarization"
      contains: "confidenceLabel"
    - path: "src/frontend/components/changes-view.tsx"
      provides: "Analyst-facing change watch presentation"
      contains: "Source posture"
    - path: "src/frontend/components/ask-view.tsx"
      provides: "Anton Rx chat experience without old FHIR/upload-centric framing"
      contains: "Natural-language policy intelligence"
---

<objective>
Finish the Anton Rx pivot by polishing the UX and backend semantics so the product reads as a credible analyst tool rather than a repurposed demo. Emphasize provenance, confidence, and clear comparison/change-watch storytelling.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Strengthen backend semantics</name>
  <files>
    src/server/antonrx-data.ts
    src/server/antonrx-routes.ts
    src/server/chat.ts
  </files>
  <action>
Add stronger provenance and confidence semantics to the Anton Rx match/detail/change data. Where the final package is broad but weakly medical-benefit-specific, expose that clearly. Where legacy structured policies provide deeper evidence, elevate that clearly.
  </action>
  <verify>
Compare/detail/chat responses expose confidence/provenance without ambiguity.
  </verify>
</task>

<task type="auto">
  <name>Task 2: Polish the analyst workspace</name>
  <files>
    src/frontend/App.tsx
    src/frontend/components/ask-view.tsx
    src/frontend/components/changes-view.tsx
    src/frontend/components/compare-view.tsx
    src/frontend/components/policy-list.tsx
    src/frontend/data/antonrx.ts
    src/frontend/styles.css
  </files>
  <action>
Tighten the visual and workflow story so the user can move fluidly from search to comparison to detail to change-watch. Remove any remaining misleading upload/readiness emphasis from the main Anton Rx flow.
  </action>
  <verify>
Frontend builds and the primary analyst workflows are clear without explanation.
  </verify>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit`
2. `npm run frontend:build`
3. live compare/detail/chat verification
</verification>

<output>
After completion, create `.planning/phases/05.1-anton-rx-demo-frontend/05.1-05-SUMMARY.md`
</output>
