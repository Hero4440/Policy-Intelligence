---
phase: 05.1-anton-rx-demo-frontend
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - src/frontend/main.tsx
  - src/frontend/App.tsx
  - src/frontend/styles.css
  - src/frontend/data/policies.ts
  - src/frontend/components/workspace-shell.tsx
  - src/frontend/components/sidebar.tsx
  - src/frontend/components/policy-list.tsx
autonomous: true

must_haves:
  truths:
    - "A frontend app can be started locally from this repo without disturbing the MCP server entrypoint"
    - "The app uses the normalized policy JSON dataset as its source of truth"
    - "The UI presents a Prompt Opinion-inspired split workspace with search, navigation, and detail areas"
    - "The visual shell feels intentional and demo-ready rather than like a default starter app"
  artifacts:
    - path: "src/frontend/App.tsx"
      provides: "Frontend app entry and workspace composition"
      contains: "PolicyPilot"
    - path: "src/frontend/data/policies.ts"
      provides: "Policy dataset adapter for frontend consumption"
      contains: "policies-index"
    - path: "src/frontend/styles.css"
      provides: "Frontend visual system and layout styling"
      contains: "--"
  key_links:
    - from: "src/frontend/data/policies.ts"
      to: "data/policies/structured/policies-index.json"
      via: "Frontend reads normalized policy coverage records"
      pattern: "payer|drug"
    - from: "src/frontend/App.tsx"
      to: "src/frontend/components/workspace-shell.tsx"
      via: "Top-level Anton Rx workspace composition"
      pattern: "Workspace"
---

<objective>
Create the frontend foundation for an Anton Rx demo surface that feels close to a clinical workspace and is powered by the normalized policy data already in this repo.

Purpose: The current project proves the backend and Prompt Opinion integration path, but Anton Rx needs a direct, visual product surface for searchable and comparable medical-benefit drug policy coverage.
Output: Frontend scaffold, policy data adapter, and a Prompt Opinion-inspired workspace shell that can be run locally.
</objective>

<execution_context>
@/Users/hero4440/.claude/get-shit-done/workflows/execute-plan.md
@/Users/hero4440/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@data/policies/structured/policies-index.json
@data/policies/structured/uhc-adalimumab-ra.json
@data/policies/structured/aetna-adalimumab-ra.json
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold the frontend app and local run path</name>
  <files>
    package.json
    tsconfig.json
    src/frontend/main.tsx
    src/frontend/App.tsx
  </files>
  <action>
Add a lightweight frontend stack appropriate for hackathon velocity. Use React + Vite + TypeScript unless repo constraints strongly force another minimal setup.

Requirements:
- Keep the existing MCP server entrypoint intact
- Add a separate frontend dev command
- Ensure the frontend can import or adapt the normalized policy data without duplicating the source JSON manually
- Avoid overengineering routing or state management; the goal is a strong demo shell, not a framework showcase
  </action>
  <verify>
Run the frontend locally and confirm the app boots without TypeScript errors.
  </verify>
  <done>
Frontend app starts locally and renders a non-empty workspace shell backed by repo data.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build a Prompt Opinion-inspired workspace shell and policy data adapter</name>
  <files>
    src/frontend/styles.css
    src/frontend/data/policies.ts
    src/frontend/components/workspace-shell.tsx
    src/frontend/components/sidebar.tsx
    src/frontend/components/policy-list.tsx
    src/frontend/App.tsx
  </files>
  <action>
Build a visual shell that evokes Prompt Opinion's left-nav + workspace layout without copying it literally. The shell should include:
- a left navigation rail
- a top workspace header
- a central content region
- a list/table or card view of available payer/drug policy entries

Use the normalized policy data as the data source. Prioritize readable structure, crisp typography, and an intentional clinical-operations feel over generic dashboard boilerplate.
  </action>
  <verify>
The rendered shell shows policy entries from the real dataset and supports at least basic drug/payer filtering.
  </verify>
  <done>
There is a credible demo shell that looks like a real product surface and is grounded in the repo's structured policy data.
  </done>
</task>

</tasks>

<verification>
1. Frontend app starts locally
2. TypeScript passes
3. Workspace shell renders data from normalized policy files
4. Existing MCP server entrypoint remains intact
</verification>

<success_criteria>
- Frontend foundation exists and is runnable
- Visual shell is demo-ready and track-appropriate
- Data adapter uses the real policy dataset rather than hard-coded dummy content
</success_criteria>

<output>
After completion, create `.planning/phases/05.1-anton-rx-demo-frontend/05.1-01-SUMMARY.md`
</output>
