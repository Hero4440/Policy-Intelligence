---
phase: 06-policy-management-ingestion
plan: 03
type: execute
wave: 2
depends_on:
  - "06-01"
  - "06-02"
files_modified:
  - src/frontend/pages/PolicyDetailPage.tsx
  - src/frontend/App.tsx
autonomous: true
requirements:
  - PLCY-04
  - PLCY-07

must_haves:
  truths:
    - "Clicking a policy in the list opens its Detail page"
    - "Detail page shows tabs: Overview, Products, Indications, Criteria, Evidence, Versions"
    - "Each tab renders the relevant section of the PolicyRecord — not empty, not placeholder"
    - "Versions tab lists every saved version with date and allows clicking to view that version's content"
    - "Selecting a past version from the Versions tab reloads the detail with that version's data"
  artifacts:
    - path: "src/frontend/pages/PolicyDetailPage.tsx"
      provides: "Policy Detail page with 6-tab layout driven by PolicyRecord data"
      exports: ["PolicyDetailPage"]
  key_links:
    - from: "src/frontend/pages/PolicyDetailPage.tsx"
      to: "/api/policies/:policyId"
      via: "fetchPolicyDetail() in useEffect on policyId/version change"
      pattern: "fetchPolicyDetail"
    - from: "src/frontend/pages/PolicyDetailPage.tsx"
      to: "/api/policies/:policyId/versions"
      via: "fetchPolicyVersions() called on mount"
      pattern: "fetchPolicyVersions"
    - from: "Versions tab version selector"
      to: "fetchPolicyDetail(policyId, selectedVersion)"
      via: "onClick triggers version state change which triggers re-fetch"
      pattern: "setSelectedVersion"
---

<objective>
Build the Policy Detail page with a 6-tab layout (Overview, Products, Indications, Criteria, Evidence, Versions). The Versions tab shows version history and allows selecting any past version to inspect its content.

Purpose: Satisfies PLCY-04 and PLCY-07 — the complete read-only policy inspection experience.
Output: src/frontend/pages/PolicyDetailPage.tsx, App.tsx wired to render it
</objective>

<execution_context>
@/Users/tanmaybhuskute/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tanmaybhuskute/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/frontend/App.tsx
@src/frontend/data/policies.ts
@src/frontend/components/workspace-shell.tsx
@src/frontend/styles.css
@src/storage/types.ts
@.planning/phases/06-policy-management-ingestion/06-01-SUMMARY.md
@.planning/phases/06-policy-management-ingestion/06-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: PolicyDetailPage with 6 tabs</name>
  <files>src/frontend/pages/PolicyDetailPage.tsx</files>
  <action>
    Create `src/frontend/pages/PolicyDetailPage.tsx` exporting `PolicyDetailPage`.

    Props:
    ```typescript
    interface PolicyDetailPageProps {
      policyId: string;
      onBack: () => void;
      onEditPolicy: (policyId: string) => void;
    }
    ```

    State:
    - `activeTab: 'overview' | 'products' | 'indications' | 'criteria' | 'evidence' | 'versions'` (default `'overview'`)
    - `record: unknown | null` — raw PolicyRecord from API (type as `any` internally to avoid cross-boundary type import issues)
    - `entry: PolicyIndexEntry | null` — from policies.ts
    - `versions: number[]` — from fetchPolicyVersions
    - `currentVersion: number`
    - `selectedVersion: number` — starts as currentVersion
    - `isLoading: boolean`
    - `error: string | null`

    On mount and on `[policyId, selectedVersion]` change:
    - Call `fetchPolicyVersions(policyId)` once (on policyId change only)
    - Call `fetchPolicyDetail(policyId, selectedVersion)` and set `record` and `entry`

    **Tabs bar** (use existing `detail-tabs` CSS pattern from src/frontend/components/detail-tabs.tsx as visual reference):
    ```
    <div className="detail-tabs">
      {(['overview','products','indications','criteria','evidence','versions'] as const).map(tab => (
        <button key={tab} className={`detail-tab${activeTab === tab ? ' detail-tab-active' : ''}`}
          onClick={() => setActiveTab(tab)}>{tab.charAt(0).toUpperCase() + tab.slice(1)}</button>
      ))}
    </div>
    ```

    **Tab content (render based on activeTab):**

    Tab `overview`: Show `entry.payer`, `entry.title`, `entry.drugFamily`, `record.policyTitle`, `record.coverageStatus`, `record.paRequired`, `record.indication` — each in a `<article className="detail-card">`.

    Tab `products`: Show `record.drug.brandName`, `record.drug.genericName`, `record.drug.aliases` (if present). Show each alias as a small chip. If no products, show empty-state.

    Tab `indications`: Render `record.indications` as a list of `<article className="detail-card">` rows. If `record.indications` is empty, show empty-state "No indications extracted."

    Tab `criteria`: Render `record.diagnosisRequirements` (each as a card with `description` + `evidenceText`). Then render `record.stepTherapy.details` as a list. Then render `record.otherRequirements` as a list. Each section has an `<h4>` heading.

    Tab `evidence`: Render `record.evidenceSnippets` as cards showing `field`, `text`, `section`, `page`. If empty, show empty-state.

    Tab `versions`: Render versions array as a list. Current version highlighted. Clicking a version number calls `setSelectedVersion(v)`. Each version row shows: `v{N}` and the version's date from `entry.versions` (note: versions array is numbers — show "Version {N}" and `entry.lastUpdatedAt` for current, "Saved version" for others). Add an "Edit Current Version" button calling `onEditPolicy(policyId)`.

    **Header area:**
    ```
    <div className="panel-header">
      <div>
        <button className="page-nav-btn" onClick={onBack}>← Back to Policy Rules</button>
        <p className="eyebrow">{entry?.payer ?? '—'}</p>
        <h2>{entry?.title ?? 'Loading...'}</h2>
      </div>
      <button className="page-nav-btn page-nav-btn-active" onClick={() => onEditPolicy(policyId)}>
        Edit Rules
      </button>
    </div>
    ```

    Import `fetchPolicyDetail`, `fetchPolicyVersions`, `PolicyIndexEntry` from `'../data/policies.js'`.
    Access record fields using `(record as any).field` pattern — avoids cross-boundary type import.
  </action>
  <verify>
    `npx tsc --noEmit` passes.
    Start dev stack: `npm run server:dev` + `npm run frontend:dev`.
    Upload a PDF via curl or the UI and navigate to its detail page.
    All 6 tabs render without runtime errors. Versions tab shows "Version 1".
  </verify>
  <done>
    PolicyDetailPage renders all 6 tabs. Overview/Products/Indications/Criteria/Evidence tabs show data from the PolicyRecord. Versions tab shows version list with clickable rows. Selecting a version calls setSelectedVersion. TypeScript clean.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire PolicyDetailPage into App.tsx</name>
  <files>src/frontend/App.tsx</files>
  <action>
    Update `src/frontend/App.tsx` to render `PolicyDetailPage` when `activePage === 'policy-detail'`.

    1. Add import: `import { PolicyDetailPage } from './pages/PolicyDetailPage.js';`

    2. In the listPane slot: when `activePage === 'policy-detail'`, render the PolicyDetailPage in the full detail area (not the list pane — this page is full-width). The simplest approach: when activePage is `'policy-detail'`, render `<PolicyDetailPage>` in the `detailPane` slot and render nothing meaningful in the `listPane` (empty div or breadcrumb). Alternatively, render PolicyDetailPage as the sole content spanning both panes by putting it in a wrapper div in `detailPane` with `style={{ gridColumn: 'span 2' }}` — but check WorkspaceShell to see if that's feasible.

    Simplest viable approach:
    - When `activePage === 'policy-detail'`, in listPane render: `<div className="empty-state" style={{padding: '1rem'}}>Select a tab to view policy detail.</div>`
    - In detailPane render:
      ```tsx
      <PolicyDetailPage
        policyId={selectedPolicyId}
        onBack={() => setActivePage('policy-rules')}
        onEditPolicy={(id) => { setSelectedPolicyId(id); setActivePage('policy-editor'); }}
      />
      ```

    3. Ensure `selectedPolicyId` state exists (added in Plan 02 — if not yet present, add it now: `const [selectedPolicyId, setSelectedPolicyId] = useState('')`).

    4. The `onSelectPolicy` handler in PolicyRulesPage already sets `selectedPolicyId` and navigates to `'policy-detail'` (set up in Plan 02).
  </action>
  <verify>
    `npm run frontend:build` passes.
    Navigate to Policy Rules → click a policy card → Detail page opens with tab bar visible.
    Clicking "← Back to Policy Rules" returns to the list.
    Clicking "Edit Rules" navigates to `'policy-editor'` (placeholder for now from Plan 02).
  </verify>
  <done>
    Clicking a policy card opens PolicyDetailPage. All 6 tabs are accessible. Back button works. Edit button navigates to policy-editor state. Build is clean.
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with zero errors
- `npm run frontend:build` succeeds
- Clicking a policy in Policy Rules navigates to the Detail page
- All 6 tabs (Overview, Products, Indications, Criteria, Evidence, Versions) render without errors
- Versions tab shows "Version 1" (or more if multiple versions exist)
- Clicking a version in the Versions tab re-fetches and re-displays that version's data
- Back button returns to Policy Rules page
</verification>

<success_criteria>
- PolicyDetailPage exists with all 6 tabs fully rendering policy data
- Versions tab implements version selection (PLCY-07)
- Navigation wired: list → detail → back to list, detail → editor
- Zero TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/06-policy-management-ingestion/06-03-SUMMARY.md` with:
- Tab content mapping (which PolicyRecord field maps to which tab)
- How versions are displayed and selected
- Any CSS class additions
</output>
