---
phase: 06-policy-management-ingestion
plan: 05
type: execute
wave: 3
depends_on:
  - "06-02"
  - "06-03"
  - "06-04"
files_modified:
  - src/frontend/pages/PolicyUploadPanel.tsx
  - src/frontend/App.tsx
autonomous: false
requirements:
  - PLCY-01
  - PLCY-02
  - PLCY-03
  - PLCY-04
  - PLCY-05
  - PLCY-06
  - PLCY-07

must_haves:
  truths:
    - "User uploads a PDF via the portal and within seconds sees a parsed structured record appear in the Policy Rules page list"
    - "Policy Rules page shows all loaded policies with working payer, drug family, and status filters"
    - "Clicking a policy opens its Detail page with all 6 tabs populated"
    - "Structured Rules Editor allows field editing, ambiguity flagging, and saving a new version"
    - "Version history shows every saved version and selecting one shows its content"
  artifacts:
    - path: "src/frontend/pages/PolicyUploadPanel.tsx"
      provides: "PDF upload UI with file picker, progress, and success redirect to Policy Rules"
      exports: ["PolicyUploadPanel"]
  key_links:
    - from: "src/frontend/pages/PolicyUploadPanel.tsx"
      to: "uploadPolicyPdf() in policies.ts"
      via: "file input onChange -> read as ArrayBuffer -> uploadPolicyPdf"
      pattern: "uploadPolicyPdf"
    - from: "PolicyUploadPanel upload success"
      to: "setActivePage('policy-rules')"
      via: "onUploaded callback in App.tsx"
      pattern: "onUploaded"
---

<objective>
Add the PDF upload UI panel and do a full end-to-end integration pass: wire the upload panel into the nav, verify all policy pages connect correctly, and confirm the complete policy lifecycle works: upload -> list -> detail -> editor -> versioned detail.

Purpose: Closes the loop on all 7 PLCY requirements — the portal's policy lifecycle is fully operational.
Output: PolicyUploadPanel component, final App.tsx navigation wiring, human verification of the complete flow
</objective>

<execution_context>
@/Users/tanmaybhuskute/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tanmaybhuskute/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/frontend/App.tsx
@src/frontend/data/policies.ts
@src/frontend/components/ingestion-panel.tsx
@src/frontend/styles.css
@.planning/phases/06-policy-management-ingestion/06-01-SUMMARY.md
@.planning/phases/06-policy-management-ingestion/06-02-SUMMARY.md
@.planning/phases/06-policy-management-ingestion/06-03-SUMMARY.md
@.planning/phases/06-policy-management-ingestion/06-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: PDF upload UI panel and App.tsx nav wiring</name>
  <files>src/frontend/pages/PolicyUploadPanel.tsx, src/frontend/App.tsx</files>
  <action>
    Create `src/frontend/pages/PolicyUploadPanel.tsx` exporting `PolicyUploadPanel`.

    Props:
    ```typescript
    interface PolicyUploadPanelProps {
      onUploaded: (policyId: string) => void;
    }
    ```

    State: `isUploading: boolean`, `uploadError: string | null`, `uploadedPolicyId: string | null`.

    Render a file upload form using existing CSS classes (panel-header, detail-card, eyebrow, chat-error, page-nav-btn, page-nav-btn-active):
    - Header: eyebrow "Policy Management", h2 "Upload Policy PDF"
    - Card with file input (`accept=".pdf,application/pdf"`, disabled when uploading)
    - Show "Parsing and saving policy..." text while uploading
    - Show uploadError in `.chat-error` div on failure
    - On success: show policyId and a "View in Policy Rules" button that calls `onUploaded(uploadedPolicyId)`

    handleFileChange handler:
    ```typescript
    async function handleFileChange(e: React.ChangeEvent<HTMLInputElement>) {
      const file = e.target.files?.[0];
      if (!file) return;
      setIsUploading(true);
      setUploadError(null);
      setUploadedPolicyId(null);
      try {
        const buffer = await file.arrayBuffer();
        const { policyId } = await uploadPolicyPdf(file.name, buffer);
        setUploadedPolicyId(policyId);
      } catch (err) {
        setUploadError(err instanceof Error ? err.message : 'Upload failed');
      } finally {
        setIsUploading(false);
      }
    }
    ```

    Import `uploadPolicyPdf` from `'../data/policies.js'`.

    App.tsx updates:
    1. Import PolicyUploadPanel from `'./pages/PolicyUploadPanel.js'`
    2. Add `'policy-upload'` to the activePage union type
    3. Add "Upload Policy" nav button in `<nav className="page-nav">`
    4. When activePage === 'policy-upload', render PolicyUploadPanel with `onUploaded={(id) => { setSelectedPolicyId(id); setActivePage('policy-rules'); }}` in the detailPane
    5. Final nav order: Workspace | Compare | Data | Policy Rules | Upload Policy
  </action>
  <verify>
    `npm run frontend:build` passes zero errors.
    Navigate to "Upload Policy" in the nav — file picker renders.
    Select a real PDF file — upload completes within seconds and shows success with a policyId.
    Click "View in Policy Rules" — navigates to Policy Rules and the new policy card appears in the list.
  </verify>
  <done>
    Upload panel functional: PDF file input -> upload -> policyId returned -> navigation to Policy Rules shows the policy.
    Zero TypeScript errors. Build passes.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verify complete policy lifecycle</name>
  <files>none</files>
  <action>
    No code changes. Human tests the full policy lifecycle end-to-end across all 5 plans built in Phase 6.
  </action>
  <verify>
    Start the dev stack: `npm run server:dev` in one terminal, `npm run frontend:dev` in another.
    Open http://localhost:4173 and run all 5 test scenarios described in how-to-verify.
  </verify>
  <done>All 5 test scenarios pass with no blocking errors.</done>
  <what-built>
    Complete policy management lifecycle across 5 plans:
    - Plan 01: POST /api/policies/upload (PDF ingestion) + GET /api/policies + GET /api/policies/:id + GET /api/policies/:id/versions
    - Plan 02: Policy Rules page with payer and drug family filters
    - Plan 03: Policy Detail page with 6 tabs (Overview, Products, Indications, Criteria, Evidence, Versions)
    - Plan 04: Structured Rules Editor with field editing, ambiguity flags, and save new version
    - Plan 05: PDF upload UI panel and complete App.tsx navigation wiring
  </what-built>
  <how-to-verify>
    Start the dev stack: `npm run server:dev` in one terminal, `npm run frontend:dev` in another.
    Open http://localhost:4173 in your browser.

    Test 1 — Upload a PDF (PLCY-01, PLCY-02):
    1. Click "Upload Policy" in the nav
    2. Select any PDF policy document from your machine
    3. Confirm upload completes within a few seconds with a policyId shown
    4. Click "View in Policy Rules" — the policy appears in the list

    Test 2 — Policy Rules filters (PLCY-03):
    1. Click "Policy Rules" in the nav
    2. Confirm the uploaded policy appears as a card with payer, title, drug family
    3. Use the payer dropdown to filter — list narrows to matching payer
    4. Use the drug family dropdown — list narrows further
    5. Clear filters — full list returns

    Test 3 — Policy Detail tabs (PLCY-04):
    1. Click the policy card — Detail page opens
    2. Click through all 6 tabs: Overview, Products, Indications, Criteria, Evidence, Versions
    3. Confirm each tab renders without errors (sparse fields from PDF heuristics are acceptable)
    4. Versions tab shows "Version 1"

    Test 4 — Structured Rules Editor + save version (PLCY-05, PLCY-06):
    1. Click "Edit Rules" button on the Detail page
    2. Structured Rules Editor opens with field rows, edit inputs, and ambiguity checkboxes
    3. Edit at least one field (e.g., change Payer name)
    4. Check the "Ambiguous" checkbox on one field
    5. Click "Save New Version"
    6. Confirm success message with "Version 2"

    Test 5 — Version history (PLCY-07):
    1. Click "Back to Detail" from the editor
    2. Click the "Versions" tab
    3. Confirm both "Version 1" and "Version 2" are listed
    4. Click "Version 1" — detail reloads with original content
    5. Click "Version 2" — detail reloads with edited content
  </how-to-verify>
  <resume-signal>Type "approved" if all 5 tests pass, or describe which tests failed and what you saw</resume-signal>
</task>

</tasks>

<verification>
All 5 test scenarios pass:
1. PDF upload completes within seconds and appears in Policy Rules list
2. Filters narrow the policy list correctly
3. All 6 Detail page tabs render
4. Fields are editable, ambiguity flags work, saving creates Version 2
5. Version history shows both versions; selecting either loads correct content
</verification>

<success_criteria>
- Full policy lifecycle operational: upload -> list with filters -> detail tabs -> editor -> versioned history
- All 7 PLCY requirements satisfied and human-verified
- Zero TypeScript errors, clean build
</success_criteria>

<output>
After completion, create `.planning/phases/06-policy-management-ingestion/06-05-SUMMARY.md` with:
- Final nav page states in App.tsx
- Upload flow summary
- Any parsing quality notes (which fields were consistently extracted vs sparse)
- UAT result (what passed, any minor issues found)
</output>
