# 05-01 Summary

- Added `src/storage/types.ts` with shared storage entity types for policies, diffs, patient cases, and evaluations.
- Added `src/storage/paths.ts` with centralized runtime data paths and `ensureDataDirectories()`.
- Existing `.gitignore` already excluded `data/`, so no change was needed there.
