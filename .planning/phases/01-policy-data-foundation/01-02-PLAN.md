---
phase: 01-policy-data-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - data/policies/structured/bcbs-nc-bevacizumab-oncology.json
  - data/policies/structured/cigna-rituximab-nononcology.json
  - data/policies/structured/policies-index.json
autonomous: true

must_haves:
  truths:
    - "BCBS NC bevacizumab policy loads with preferred/non-preferred product tiers, step therapy, prior auth criteria, and per-field evidence snippets with page/section references"
    - "Cigna rituximab non-oncology policy loads with prior auth criteria, step therapy, covered indications, and per-field evidence snippets with page/section references"
    - "Both new policies validate against the extended Zod schema without errors"
    - "Policy index includes all 7 policies (5 RA + 2 new) and loader reads them all at startup"
    - "Every extracted field in both new policies has 1-3 sentence evidenceText with source page and section reference"
  artifacts:
    - path: "data/policies/structured/bcbs-nc-bevacizumab-oncology.json"
      provides: "Normalized BCBS NC oncology policy with evidence"
      contains: "bevacizumab"
    - path: "data/policies/structured/cigna-rituximab-nononcology.json"
      provides: "Normalized Cigna rituximab policy with evidence"
      contains: "rituximab"
    - path: "data/policies/structured/policies-index.json"
      provides: "Updated index with all 7 policies"
      contains: "bcbs-nc-bevacizumab-oncology"
  key_links:
    - from: "data/policies/structured/policies-index.json"
      to: "src/mcp/policy_store/loader.ts"
      via: "index.policies array drives file loading loop"
      pattern: "policies-index.json"
    - from: "data/policies/structured/bcbs-nc-bevacizumab-oncology.json"
      to: "data/schemas/policy.schema.ts"
      via: "PolicyRecordSchema.parse() validation at load time"
      pattern: "PolicyRecordSchema.parse"
---

<objective>
Extract and normalize BCBS NC bevacizumab oncology policy and Cigna rituximab non-oncology policy into structured JSON with per-field evidence mapping. Update the policy index so the loader picks up all 7 policies.

Purpose: Completes Phase 1 by creating the actual policy data files that downstream MCP tools (list, summary, compare, Q&A) will serve. Without these files, no policy intelligence is possible.

Output: 2 new policy JSON files with evidence mappings, updated policies-index.json with 7 entries, all validating against extended schema.
</objective>

<execution_context>
@/Users/hero4440/.claude/get-shit-done/workflows/execute-plan.md
@/Users/hero4440/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-policy-data-foundation/01-01-SUMMARY.md
@data/schemas/policy.schema.ts
@data/policies/structured/uhc-adalimumab-ra.json
@data/policies/structured/policies-index.json
@docs/hackaathon/Medical Drug Coverage Policy Examples/BCBS NC - Corporate Medical Policy_ Preferred Injectable Oncology Program (Avastin example).pdf
@docs/hackaathon/Medical Drug Coverage Policy Examples/Cigna Rituximab Intravenous Products for Non-Oncology Indications.pdf
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract and create BCBS NC bevacizumab oncology policy JSON</name>
  <files>data/policies/structured/bcbs-nc-bevacizumab-oncology.json</files>
  <action>
**Step 1: Extract text from BCBS NC PDF.**

Run the existing PDF extraction tool or read the PDF directly:
```bash
npx tsx src/extraction/pdf-extractor.ts "docs/hackaathon/Medical Drug Coverage Policy Examples/BCBS NC - Corporate Medical Policy_ Preferred Injectable Oncology Program (Avastin example).pdf"
```

If the extraction script doesn't support CLI args, read the PDF directly using the Read tool (it supports PDFs). Review the extracted text to identify:
- Preferred vs non-preferred bevacizumab products (look for tier tables)
- Prior authorization requirements
- Step therapy / fail-first requirements (preferred product trial before non-preferred)
- Covered indications (oncology indications for bevacizumab)
- Notable restrictions

**Step 2: Create structured JSON file.**

Create `data/policies/structured/bcbs-nc-bevacizumab-oncology.json` following the existing RA policy pattern but using the new oncology fields. Structure:

```json
{
  "id": "bcbs-nc-bevacizumab-oncology",
  "payer": "BCBS-NC",
  "plan": "Commercial",
  "policyTitle": "[Full policy title from PDF]",
  "drug": {
    "brandName": "Avastin",
    "genericName": "bevacizumab",
    "aliases": ["Mvasi", "Zirabev", "Alymsys", "Vegzelma", "Avzivi"],
    "products": [
      { "name": "Mvasi (bevacizumab-awwb)", "tier": "preferred", "aliases": ["bevacizumab-awwb"] },
      { "name": "Zirabev (bevacizumab-bvzr)", "tier": "preferred", "aliases": ["bevacizumab-bvzr"] },
      { "name": "Avastin", "tier": "non-preferred", "aliases": [] }
    ]
  },
  "indication": "Oncology",
  "indications": ["[list specific oncology indications from PDF]"],
  "coverageStatus": "covered-with-pa",
  "paRequired": true,
  "diagnosisRequirements": [
    {
      "icd10Codes": ["[relevant codes if listed]"],
      "description": "[diagnosis description from PDF]",
      "evidenceText": "[1-3 sentence lightly edited quote from PDF]",
      "source": { "document": "[PDF filename]", "page": N, "section": "[section title]" }
    }
  ],
  "stepTherapy": [
    {
      "drugName": "[preferred bevacizumab biosimilar requirement]",
      "duration": "[trial duration]",
      "failureCriteria": "[failure criteria from PDF]",
      "evidenceText": "[1-3 sentence quote about step therapy]",
      "source": { "document": "[PDF filename]", "page": N, "section": "[section title]" }
    }
  ],
  "otherRequirements": [
    {
      "category": "preferred product requirement",
      "requirement": "[structured summary of preferred vs non-preferred rules]",
      "evidenceText": "[1-3 sentence quote]",
      "source": { "document": "[PDF filename]", "page": N, "section": "[section title]" },
      "ambiguous": false
    }
  ],
  "sourceDocument": {
    "filename": "BCBS NC - Corporate Medical Policy_ Preferred Injectable Oncology Program (Avastin example).pdf",
    "retrievalDate": "2026-04-12",
    "effectiveDate": "[from PDF if available]"
  }
}
```

**Evidence mapping rules (per user decision):**
- Every field (preferred status, step therapy, restrictions, diagnosis, etc.) gets its own `evidenceText` with 1-3 sentences
- Use lightly edited direct quotes from the PDF for evidence accuracy
- Include `page` (PDF page number, not document page number) and `section` (section title) for traceability
- If any field is unclear or ambiguous, set `ambiguous: true` with explanation in the evidence note
- Products array must have `tier: 'preferred'` or `tier: 'non-preferred'` per user decision on tier structure
  </action>
  <verify>
Validate against schema: `npx tsx -e "import { PolicyRecordSchema } from './data/schemas/policy.schema.ts'; import { readFileSync } from 'fs'; const data = JSON.parse(readFileSync('data/policies/structured/bcbs-nc-bevacizumab-oncology.json', 'utf-8')); const result = PolicyRecordSchema.parse(data); console.log('BCBS NC policy validates'); console.log('Products:', result.drug.products?.length); console.log('Indications:', result.indications?.length); console.log('Evidence count:', result.stepTherapy.length + result.diagnosisRequirements.length + result.otherRequirements.length);"` — must validate, have products with tiers, and have evidence on every field.

Spot-check evidence: Every `evidenceText` field should be 1-3 sentences and every `source` should have page and section.
  </verify>
  <done>
- bcbs-nc-bevacizumab-oncology.json exists with all required fields
- Products array contains preferred (biosimilars) and non-preferred (Avastin) entries with tier field
- Multiple oncology indications listed in indications array
- Step therapy captures preferred-product-first requirement
- Every field has evidenceText (1-3 sentences) with page/section source reference
- Validates against extended PolicyRecordSchema
  </done>
</task>

<task type="auto">
  <name>Task 2: Extract and create Cigna rituximab non-oncology policy JSON</name>
  <files>data/policies/structured/cigna-rituximab-nononcology.json</files>
  <action>
**Step 1: Extract text from Cigna PDF.**

Run extraction or read the PDF directly:
```bash
npx tsx src/extraction/pdf-extractor.ts "docs/hackaathon/Medical Drug Coverage Policy Examples/Cigna Rituximab Intravenous Products for Non-Oncology Indications.pdf"
```

If the extraction script doesn't support CLI args, read the PDF using the Read tool. Review extracted text to identify:
- Prior authorization requirements for rituximab IV
- Step therapy / fail-first requirements
- Covered non-oncology indications (e.g., RA, granulomatosis with polyangiitis, pemphigus vulgaris, etc.)
- Preferred vs non-preferred products (if Cigna distinguishes biosimilars)
- Notable restrictions or limitations

**Step 2: Create structured JSON file.**

Create `data/policies/structured/cigna-rituximab-nononcology.json`:

```json
{
  "id": "cigna-rituximab-nononcology",
  "payer": "Cigna",
  "plan": "Commercial",
  "policyTitle": "[Full policy title from PDF]",
  "drug": {
    "brandName": "Rituxan",
    "genericName": "rituximab",
    "aliases": ["Truxima", "Ruxience", "Riabni"],
    "products": [
      // Include products with tiers IF Cigna distinguishes preferred/non-preferred
      // If Cigna treats all rituximab products equally, omit products array
    ]
  },
  "indication": "Non-Oncology",
  "indications": ["[list specific non-oncology indications from PDF]"],
  "coverageStatus": "[covered or covered-with-pa based on PDF]",
  "paRequired": true,
  "diagnosisRequirements": [
    // Extract all diagnosis/indication requirements with evidence
  ],
  "stepTherapy": [
    // Extract step therapy requirements with evidence
  ],
  "otherRequirements": [
    // Extract other requirements (lab values, prescriber qualifications, etc.)
  ],
  "sourceDocument": {
    "filename": "Cigna Rituximab Intravenous Products for Non-Oncology Indications.pdf",
    "retrievalDate": "2026-04-12",
    "effectiveDate": "[from PDF if available]"
  }
}
```

**Same evidence mapping rules as Task 1:**
- Every field gets `evidenceText` (1-3 sentences, lightly edited direct quotes)
- Include `page` and `section` in every source reference
- Mark `ambiguous: true` with explanation if evidence is unclear
- Since this is a non-oncology policy, the indications array should focus on autoimmune/non-oncology conditions
  </action>
  <verify>
Validate against schema: `npx tsx -e "import { PolicyRecordSchema } from './data/schemas/policy.schema.ts'; import { readFileSync } from 'fs'; const data = JSON.parse(readFileSync('data/policies/structured/cigna-rituximab-nononcology.json', 'utf-8')); const result = PolicyRecordSchema.parse(data); console.log('Cigna policy validates'); console.log('Indications:', result.indications?.length); console.log('Evidence count:', result.stepTherapy.length + result.diagnosisRequirements.length + result.otherRequirements.length);"` — must validate with non-oncology indications and per-field evidence.

Spot-check: Every `evidenceText` field should be 1-3 sentences and every `source` should have page and section.
  </verify>
  <done>
- cigna-rituximab-nononcology.json exists with all required fields
- Non-oncology indications listed (autoimmune conditions)
- Prior auth criteria captured with evidence
- Step therapy requirements (if any) captured with evidence
- Every field has evidenceText (1-3 sentences) with page/section source reference
- Validates against extended PolicyRecordSchema
  </done>
</task>

<task type="auto">
  <name>Task 3: Update policy index and validate full policy suite</name>
  <files>data/policies/structured/policies-index.json</files>
  <action>
**Step 1: Update policies-index.json.**

Add the 2 new policy entries to the policies array:
```json
{
  "version": "1.0",
  "generatedDate": "2026-04-12",
  "therapeuticArea": "Rheumatoid Arthritis, Oncology, Non-Oncology",
  "policies": [
    // ... existing 5 RA policies unchanged ...
    {
      "id": "bcbs-nc-bevacizumab-oncology",
      "file": "bcbs-nc-bevacizumab-oncology.json",
      "payer": "BCBS-NC",
      "drug": "bevacizumab"
    },
    {
      "id": "cigna-rituximab-nononcology",
      "file": "cigna-rituximab-nononcology.json",
      "payer": "Cigna",
      "drug": "rituximab"
    }
  ]
}
```

**Step 2: Run full validation of all 7 policies.**

Execute a validation script that loads all 7 policies through the policy loader and confirms:
1. All 7 policies in the index load successfully
2. All 7 validate against PolicyRecordSchema
3. The 2 new policies have evidence on every field (non-empty evidenceText)
4. Drug alias resolution works for new drug families

```bash
npx tsx -e "
import { PolicyRecordSchema } from './data/schemas/policy.schema.ts';
import { normalizeDrugName } from './data/lookup/drug-aliases.ts';
import { readFileSync } from 'fs';

const index = JSON.parse(readFileSync('data/policies/structured/policies-index.json', 'utf-8'));
let passed = 0;

for (const entry of index.policies) {
  const data = JSON.parse(readFileSync('data/policies/structured/' + entry.file, 'utf-8'));
  PolicyRecordSchema.parse(data);
  console.log('PASS:', entry.id);
  passed++;
}

console.log('\n' + passed + '/' + index.policies.length + ' policies validated');

// Verify drug alias resolution
const tests = [
  ['Mvasi', 'bevacizumab'], ['Avastin', 'bevacizumab'], ['Zirabev', 'bevacizumab'],
  ['Truxima', 'rituximab'], ['Rituxan', 'rituximab'], ['Ruxience', 'rituximab'],
  ['Humira', 'adalimumab'], ['Enbrel', 'etanercept']
];
for (const [input, expected] of tests) {
  const result = normalizeDrugName(input);
  console.log(result === expected ? 'PASS' : 'FAIL', input, '->', result);
}
"
```

All 7 policies must validate and all drug alias tests must pass.
  </action>
  <verify>
The validation script in the action section IS the verification. All output lines must show PASS. Final line must read "7/7 policies validated". All drug alias tests must show PASS.

Additionally, confirm server can start: `npx tsx src/mcp/policy_store/loader.ts 2>&1 | head -5` — should show "Loaded 7 policies" without errors.
  </verify>
  <done>
- policies-index.json contains 7 entries (5 RA + BCBS NC + Cigna)
- All 7 policies validate against extended PolicyRecordSchema
- Drug alias resolution works for all 6 drug families (4 RA + bevacizumab + rituximab)
- Policy loader can load all 7 policies at startup
- Phase 1 complete: all success criteria met
  </done>
</task>

</tasks>

<verification>
1. All 7 policy JSON files validate against extended Zod schema
2. BCBS NC policy has products with preferred/non-preferred tiers and multiple oncology indications
3. Cigna policy has non-oncology indications with per-field evidence
4. Every evidenceText field in both new policies is 1-3 sentences with page/section source
5. Drug alias resolution works for all bevacizumab and rituximab family members
6. Policy loader loads all 7 policies at server startup
7. policies-index.json reflects all 7 entries
</verification>

<success_criteria>
- 2 new policy JSON files created with full evidence mapping
- Policy index updated to 7 entries covering RA + oncology + non-oncology
- All policies load and validate at server startup
- Phase 1 success criteria fully met: BCBS NC and Cigna normalized, every field has evidence, drug aliases resolve, Zod validates without errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-policy-data-foundation/01-02-SUMMARY.md`
</output>
