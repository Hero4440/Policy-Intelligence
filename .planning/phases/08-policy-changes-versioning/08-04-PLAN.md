---
phase: 08-policy-changes-versioning
plan: "04"
type: execute
wave: 3
depends_on:
  - "08-02"
  - "08-03"
files_modified:
  - src/frontend/components/policy-version-diff-view.tsx
  - src/frontend/components/policy-changes-view.tsx
  - src/frontend/App.tsx
  - src/frontend/styles.css
autonomous: false
requirements:
  - CHNG-03

must_haves:
  truths:
    - "Users can open a Version Diff screen for two versions of the same policy"
    - "The screen shows structured field changes and side-by-side text snapshots together"
    - "Phase 8 is human-verified across timeline, severity labels, and version diff flows"
  artifacts:
    - path: "src/frontend/components/policy-version-diff-view.tsx"
      provides: "Version Diff page UI"
      exports:
        - PolicyVersionDiffView
  key_links:
    - from: "src/frontend/components/policy-changes-view.tsx"
      to: "src/frontend/components/policy-version-diff-view.tsx"
      via: "event CTA opens selected version pair"
      pattern: "onOpenDiff"
    - from: "src/frontend/components/policy-version-diff-view.tsx"
      to: "/api/policies/:policyId/diff"
      via: "fetchPolicyVersionDiff"
      pattern: "fetchPolicyVersionDiff"
---

<objective>
Finish Phase 8 with the Version Diff page and a blocking browser verification pass.

Purpose: The phase is not complete until users can inspect version changes visually and verify the classification/timeline behavior in the browser.
Output: Version Diff page UI plus human-verified Phase 8 flow
</objective>

<execution_context>
@/Users/tanmaybhuskute/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tanmaybhuskute/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/08-policy-changes-versioning/08-CONTEXT.md
@.planning/phases/08-policy-changes-versioning/08-02-PLAN.md
@.planning/phases/08-policy-changes-versioning/08-03-PLAN.md
@src/frontend/App.tsx
@src/frontend/styles.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Version Diff page and navigation flow</name>
  <files>src/frontend/components/policy-version-diff-view.tsx, src/frontend/components/policy-changes-view.tsx, src/frontend/App.tsx, src/frontend/styles.css</files>
  <action>
    Create the Version Diff page UI and wire it from the Policy Changes timeline.

    Requirements:
    - user can open a diff for a selected version pair from the Policy Changes page
    - the Version Diff screen shows:
      - policy metadata header
      - version selector or version-pair context
      - structured change table with severity badges
      - side-by-side text panes for left/right versions
    - both structured changes and side-by-side text are visible on the same screen without route changes outside the main app shell
    - text panes must remain readable on a normal laptop viewport
    - clear empty/error state if a requested version pair is unavailable

    UX details:
    - severity badges should visually match the Policy Changes page
    - long text panes can scroll, but layout should not collapse
    - add a clear back action to return to the Policy Changes timeline
  </action>
  <verify>
    `npm run frontend:build` passes.
    Clicking a Version Diff action from the Changes page opens the diff screen and renders both sections without runtime errors.
  </verify>
  <done>
    The portal has a usable Version Diff workflow satisfying the UI half of CHNG-03.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verify complete Phase 8 workflow</name>
  <files>none</files>
  <action>
    No code changes. Human tests the Policy Changes and Version Diff flows end-to-end in the browser using at least one policy with multiple versions, following the dedicated checklist in `08-UAT.md`.
  </action>
  <verify>
    Start the dev stack:
    - `npm run server:dev`
    - `npm run frontend:dev`

    Open `http://localhost:4173` and run all verification scenarios in `.planning/phases/08-policy-changes-versioning/08-UAT.md`.
  </verify>
  <done>All Phase 8 scenarios pass with no blocking errors.</done>
  <resume-signal>Type "approved" if all 5 tests pass, or describe what failed and what you saw</resume-signal>
</task>

</tasks>

<verification>
All 5 Phase 8 scenarios pass:
1. Changes page renders timeline events newest-first
2. Change table exposes old/new/severity clearly
3. Materiality classification is believable in the browser
4. Version Diff page shows structured + text comparison together
5. Phase 8 nav wiring does not break existing top-level workflows
</verification>

<success_criteria>
- Phase 8 requirements are complete and human-verified
- Users can audit policy change history and inspect version diffs in one portal flow
- CHNG-03 is fully implemented end-to-end
</success_criteria>

<output>
After completion, create `.planning/phases/08-policy-changes-versioning/08-04-SUMMARY.md` with:
- Version Diff implementation summary
- end-to-end UAT results
- any remaining polish gaps that do not block Phase 9
- final Phase 8 readiness statement
</output>
