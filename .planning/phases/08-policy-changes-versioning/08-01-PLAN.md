---
phase: 08-policy-changes-versioning
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/storage/types.ts
  - src/storage/policy-store.ts
  - src/server/policy-changes.ts
  - src/server/policy-routes.ts
autonomous: true
requirements:
  - CHNG-01
  - CHNG-02
  - CHNG-04

must_haves:
  truths:
    - "The backend can list policy change events sorted newest-first across all stored version transitions"
    - "Each field-level change is classified deterministically as cosmetic, operational, or clinical"
    - "The change payload exposes policy, version pair, field path, old value, new value, severity, and rationale"
  artifacts:
    - path: "src/server/policy-changes.ts"
      provides: "Change-history aggregation and deterministic materiality classification"
      exports:
        - classifyPolicyDiffField
        - listPolicyChangeEvents
        - getPolicyChangeEvent
    - path: "src/server/policy-routes.ts"
      provides: "Policy change history API endpoints"
      exports:
        - registerPolicyRoutes
  key_links:
    - from: "src/server/policy-changes.ts"
      to: "src/storage/policy-store.ts"
      via: "reads stored versions + diff records"
      pattern: "diffPolicyVersions"
    - from: "src/server/policy-routes.ts"
      to: "src/server/policy-changes.ts"
      via: "GET /api/policies/changes"
      pattern: "listPolicyChangeEvents"
---

<objective>
Build the Phase 8 backend change-history engine: classify diffs, flatten them into analyst-readable events, and expose them through API routes.

Purpose: The UI cannot meet Phase 8 without a deterministic source of truth for timeline events and severity labels.
Output: classified change domain service plus change-history routes
</objective>

<execution_context>
@/Users/tanmaybhuskute/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tanmaybhuskute/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-policy-changes-versioning/08-CONTEXT.md
@src/storage/types.ts
@src/storage/policy-store.ts
@src/server/policy-routes.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add classified change-domain types and flattening rules</name>
  <files>src/storage/types.ts, src/server/policy-changes.ts</files>
  <action>
    Extend the storage/domain typing to support Phase 8 change classification.

    Required additions:
    - `ChangeSeverity = 'cosmetic' | 'operational' | 'clinical'`
    - `ClassifiedDiffField` shape with:
      - `field`
      - `oldValue`
      - `newValue`
      - `severity`
      - `rationale`
      - `changeType` (`added` | `removed` | `updated`)
    - `PolicyChangeEvent` shape with:
      - `policyId`
      - `policyTitle`
      - `payer`
      - `drugFamily`
      - `fromVersion`
      - `toVersion`
      - `timestamp`
      - `summary`
      - `severityCounts`
      - `changes: ClassifiedDiffField[]`

    In `src/server/policy-changes.ts`:
    - build a deterministic classifier that maps field paths to severity
    - flatten top-level diff blobs into meaningful field paths where needed, especially for:
      - `coverageStatus`
      - `paRequired`
      - `stepTherapy`
      - `indications`
      - `diagnosisRequirements`
      - `otherRequirements`
      - `drug.products`
    - classification rules:
      - `clinical`: PA, step therapy, product tier/preferred split, restrictions, indications, coverage posture
      - `operational`: payer/plan/title/metadata wording changes that change meaning but not explicit coverage posture
      - `cosmetic`: whitespace-only, ordering-only, or format-only changes
    - return a short rationale string for every classified field
  </action>
  <verify>
    `npx tsc --noEmit` passes.
    At least one synthetic diff can be classified into each of the three severities.
  </verify>
  <done>
    Phase 8 has typed, deterministic change-classification primitives that compile cleanly.
  </done>
</task>

<task type="auto">
  <name>Task 2: Expose change timeline API routes</name>
  <files>src/storage/policy-store.ts, src/server/policy-changes.ts, src/server/policy-routes.ts</files>
  <action>
    Add backend helpers and routes for Phase 8 history views.

    In `src/storage/policy-store.ts`:
    - add a helper to enumerate available diff transitions for a policy from the index/version list
    - keep the existing stored diff files as the durable source; do not introduce a database

    In `src/server/policy-changes.ts`:
    - implement `listPolicyChangeEvents(filters?)`
    - aggregate all policy transitions into newest-first events
    - support optional filters:
      - `policyId`
      - `payer`
      - `drugFamily`
      - `severity`

    In `src/server/policy-routes.ts`, add:
    - `GET /api/policies/changes`
      - returns timeline events sorted by timestamp descending
    - `GET /api/policies/:policyId/changes`
      - returns only the events for one policy

    Response requirements:
    - every event includes the version pair and flattened classified changes
    - event summary reads like a factual audit label, for example:
      - `Version 2 introduced 3 clinical and 1 operational changes`
    - if a diff file is missing for a known version pair, return a safe empty change list plus a warning field rather than crashing
  </action>
  <verify>
    Start the server and hit:
    - `curl http://localhost:3000/api/policies/changes`
    - `curl http://localhost:3000/api/policies/<policyId>/changes`
    Both routes return valid JSON and newest-first timeline ordering.
  </verify>
  <done>
    CHNG-01, CHNG-02, and CHNG-04 have backend support through classified change-history routes.
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- Change events are deterministic and newest-first
- Each field-level change exposes severity + rationale
- Missing diff artifacts fail safely instead of breaking the API
</verification>

<success_criteria>
- The backend can power both the Policy Changes page and Version Diff page
- Materiality classification is explainable and deterministic
- CHNG-01, CHNG-02, and CHNG-04 are implemented at the API/domain layer
</success_criteria>

<output>
After completion, create `.planning/phases/08-policy-changes-versioning/08-01-SUMMARY.md` with:
- classification rules implemented
- field paths treated as clinical vs operational vs cosmetic
- route shapes added
- verification results
</output>
