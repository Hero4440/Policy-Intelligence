---
phase: 08-policy-changes-versioning
plan: "02"
type: execute
wave: 1
depends_on:
  - "08-01"
files_modified:
  - src/frontend/data/policies.ts
  - src/frontend/components/policy-changes-view.tsx
  - src/frontend/App.tsx
  - src/frontend/styles.css
autonomous: true
requirements:
  - CHNG-01
  - CHNG-02

must_haves:
  truths:
    - "A top-level Policy Changes page is reachable from the portal nav"
    - "The page shows a timeline of version events sorted newest-first"
    - "Each event exposes a change table with policy, field, old value, new value, and severity"
  artifacts:
    - path: "src/frontend/components/policy-changes-view.tsx"
      provides: "Policy Changes timeline + change table UI"
      exports:
        - PolicyChangesView
    - path: "src/frontend/data/policies.ts"
      provides: "Typed Policy Changes API client helpers"
      exports:
        - fetchPolicyChanges
        - fetchPolicyChangesForPolicy
  key_links:
    - from: "src/frontend/components/policy-changes-view.tsx"
      to: "/api/policies/changes"
      via: "fetchPolicyChanges() on load and filter changes"
      pattern: "fetchPolicyChanges"
    - from: "src/frontend/App.tsx"
      to: "src/frontend/components/policy-changes-view.tsx"
      via: "top-level page nav wiring"
      pattern: "PolicyChangesView"
---

<objective>
Build the Policy Changes page UI so analysts can scan version history and field-level changes without leaving the main portal flow.

Purpose: Phase 8 requires a dedicated audit surface, not just backend routes.
Output: Policy Changes page component, typed client helpers, nav wiring, and styles
</objective>

<execution_context>
@/Users/tanmaybhuskute/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tanmaybhuskute/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-policy-changes-versioning/08-CONTEXT.md
@.planning/phases/08-policy-changes-versioning/08-01-PLAN.md
@src/frontend/App.tsx
@src/frontend/data/policies.ts
@src/frontend/styles.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend frontend policy client for change-history payloads</name>
  <files>src/frontend/data/policies.ts</files>
  <action>
    Add typed client interfaces and fetch helpers for Phase 8.

    Required additions:
    - `ChangeSeverity`
    - `ClassifiedDiffField`
    - `PolicyChangeEvent`
    - `PolicyChangesResponse`

    Export:
    - `fetchPolicyChanges(filters?)`
    - `fetchPolicyChangesForPolicy(policyId)`

    Requirements:
    - use the existing `fetchJson` helper pattern
    - support optional filter params for `payer`, `drugFamily`, `severity`, and `policyId`
    - return stable typed payloads matching the backend route shapes from `08-01`
  </action>
  <verify>`npm run frontend:build` passes with zero type errors in `src/frontend/data/policies.ts`</verify>
  <done>Phase 8 frontend has typed change-history fetch helpers.</done>
</task>

<task type="auto">
  <name>Task 2: Build the Policy Changes timeline + table view</name>
  <files>src/frontend/components/policy-changes-view.tsx, src/frontend/App.tsx, src/frontend/styles.css</files>
  <action>
    Create `src/frontend/components/policy-changes-view.tsx` and wire it into the existing top-level app shell.

    Page requirements:
    - top-level heading:
      - eyebrow: `Policy Changes`
      - title: `Version Timeline`
    - filter controls for:
      - payer
      - drug family
      - severity
    - newest-first event list
    - each event card shows:
      - policy title
      - payer
      - drug family
      - version pair (`v1 → v2`)
      - event timestamp
      - severity summary chips
    - below or within each event, render a change table with columns:
      - field
      - old value
      - new value
      - severity
      - rationale

    App wiring:
    - add a new top-level page state for Policy Changes
    - add a `Changes` nav button
    - render `PolicyChangesView` as its own workflow, parallel to Compare and Insights

    UX expectations:
    - clear empty state when there are no stored version changes yet
    - long values truncate safely but remain inspectable
    - severity colors are consistent with the classifier:
      - cosmetic = neutral
      - operational = amber
      - clinical = red
  </action>
  <verify>
    `npm run frontend:build` passes.
    Opening the app shows a `Changes` nav item and the page renders timeline events without console/runtime failures.
  </verify>
  <done>
    CHNG-01 and CHNG-02 are satisfied in the UI with a dedicated Policy Changes page.
  </done>
</task>

</tasks>

<verification>
- Policy Changes is reachable from top nav
- Timeline renders newest-first version events
- Field-level change table exposes old/new/severity clearly
- Empty state is explicit when only single-version policies exist
</verification>

<success_criteria>
- Analysts can audit policy changes without opening raw files
- CHNG-01 and CHNG-02 are implemented end-to-end
- The portal now has a dedicated Phase 8 entry point
</success_criteria>

<output>
After completion, create `.planning/phases/08-policy-changes-versioning/08-02-SUMMARY.md` with:
- UI structure shipped
- filters added
- severity display mapping
- verification results
</output>
