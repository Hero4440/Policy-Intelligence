---
phase: 05-deployment-integration
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/mcp/schemas/tool_inputs.ts
  - src/mcp/tools/check_patient_readiness.ts
  - docs/prompt-opinion/workspace-integration.md
  - docs/prompt-opinion/marketplace-listing.md
autonomous: false
user_setup:
  - "Have Prompt Opinion workspace access with permission to add MCP connections, create agents, and publish to Marketplace"
  - "Be able to sign in and perform the UI-only platform steps during execution"

must_haves:
  truths:
    - "PolicyPilot MCP connection is registered in Prompt Opinion using the public HTTPS MCP endpoint"
    - "SHARP FHIR context is enabled and validated against the deployed connection"
    - "PolicyPilot agent is configured with all three MCP tools attached"
    - "check_patient_readiness input parsing matches the actual SHARP context fields Prompt Opinion provides, or gracefully accepts both documented and observed variants"
    - "Marketplace listing exists with enough description and usage guidance for judges to discover and test the solution"
    - "End-to-end workflow succeeds inside Prompt Opinion: select patient, ask coverage question, ask criteria question, ask readiness question, receive grounded answers with policy citations"
  artifacts:
    - path: "docs/prompt-opinion/workspace-integration.md"
      provides: "Exact workspace registration steps, SHARP findings, and end-to-end validation notes"
      contains: "SHARP"
    - path: "docs/prompt-opinion/marketplace-listing.md"
      provides: "Marketplace submission copy and judge-facing usage instructions"
      contains: "PolicyPilot"
    - path: "src/mcp/tools/check_patient_readiness.ts"
      provides: "Prompt Opinion-compatible readiness parsing and graceful SHARP handling"
      contains: "patient_context"
  key_links:
    - from: "docs/prompt-opinion/workspace-integration.md"
      to: "src/mcp/tools/check_patient_readiness.ts"
      via: "Observed SHARP context fields are captured and wired into tool expectations"
      pattern: "fhir_token|patient_id|fhir_server_url|sharp"
    - from: "docs/prompt-opinion/workspace-integration.md"
      to: "docs/prompt-opinion/marketplace-listing.md"
      via: "Validated workflow becomes the judge-facing instructions"
      pattern: "coverage|criteria|readiness"
---

<objective>
Integrate the deployed MCP server into Prompt Opinion, validate patient-context propagation, publish the solution, and record the exact platform-specific findings needed for reliable demo execution.

Purpose: Deployment alone is not sufficient; Phase 5 is only complete when the server is actually usable within Prompt Opinion and discoverable by judges.
Output: Prompt Opinion workspace integration docs, any required SHARP compatibility code updates, marketplace submission copy, and end-to-end validation evidence.
</objective>

<execution_context>
@/Users/hero4440/.claude/get-shit-done/workflows/execute-plan.md
@/Users/hero4440/.claude/get-shit-done/templates/summary.md
@/Users/hero4440/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-deployment-integration/05-RESEARCH.md
@.planning/phases/04-patient-data-setup/04-01-SUMMARY.md

Relevant implementation files:
@src/mcp/schemas/tool_inputs.ts
@src/mcp/tools/check_patient_readiness.ts
@src/mcp/index.ts
@data/patients/demo-patients/README.md
@docs/hackathon1/descriiption.txt
@docs/hackathon1/youtube_transcript.txt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register the MCP connection and reconcile SHARP context behavior</name>
  <files>
    src/mcp/schemas/tool_inputs.ts
    src/mcp/tools/check_patient_readiness.ts
    docs/prompt-opinion/workspace-integration.md
  </files>
  <action>
Use the public HTTPS endpoint from 05-01 to add a new Prompt Opinion MCP connection. Record the exact UI path and connection settings in `docs/prompt-opinion/workspace-integration.md`, including:
- MCP connection name
- Public MCP endpoint URL
- Health check URL
- Confirmation that SHARP FHIR context was enabled
- Any observed Prompt Opinion wording or toggles that differ from the research assumptions

Then verify what patient-context payload actually reaches `check_patient_readiness` when called from Prompt Opinion. If the platform sends field names or nesting that differ from the current assumptions (`fhir_token`, `patient_id`, `fhir_server_url`), update `src/mcp/schemas/tool_inputs.ts` and `src/mcp/tools/check_patient_readiness.ts` so the tool accepts both the documented local shape and the observed Prompt Opinion shape. The goal is flexibility, not brittle one-off parsing.

Document the final accepted context shapes in `docs/prompt-opinion/workspace-integration.md`.
  </action>
  <verify>
Verification requires both code and platform checks:
```bash
npx tsc --noEmit
```
In Prompt Opinion, call `check_patient_readiness` for one demo patient and confirm the tool receives enough context to attempt FHIR retrieval instead of falling back to checklist-only mode. Record the observed payload details in the doc.
  </verify>
  <done>
The MCP connection exists in Prompt Opinion, SHARP context is enabled, and the readiness tool accepts the actual platform payload shape without breaking the prior local contract.
  </done>
</task>

<task type="auto">
  <name>Task 2: Configure the PolicyPilot agent and publish judge-facing Marketplace materials</name>
  <files>
    docs/prompt-opinion/workspace-integration.md
    docs/prompt-opinion/marketplace-listing.md
  </files>
  <action>
Create a PolicyPilot agent in Prompt Opinion and attach the registered MCP connection with all three tools. In `docs/prompt-opinion/workspace-integration.md`, record:
- Agent name
- Attached MCP connection
- System prompt or instruction framing used
- Which demo patients were used for validation

Create `docs/prompt-opinion/marketplace-listing.md` with the exact content needed for publishing:
- One-paragraph value proposition
- Tool/capability summary
- Short judge instructions: select demo patient, ask one coverage question, one criteria question, one readiness question
- Known limitations/caveats kept honest and concise
- Suggested discovery keywords (prior authorization, coverage, rheumatoid arthritis, FHIR, MCP)

Do not treat publication as complete until the listing is visible in Marketplace or the platform confirms the publish action succeeded.
  </action>
  <verify>
Platform verification:
- Agent config page shows all three tools attached
- Marketplace listing shows the published PolicyPilot entry

Documentation verification:
```bash
test -f docs/prompt-opinion/workspace-integration.md
test -f docs/prompt-opinion/marketplace-listing.md
```
  </verify>
  <done>
The agent is configured inside Prompt Opinion, and the Marketplace listing exists with clear judge-facing usage guidance.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Prompt Opinion MCP connection, PolicyPilot agent, Marketplace listing, and any required SHARP compatibility updates</what-built>
  <how-to-verify>Inside Prompt Opinion only, confirm three things: the MCP connection is healthy with SHARP enabled, the agent shows all three tools, and the published listing is visible. Then run the demo flow on a real demo patient and verify the responses cite policy evidence.</how-to-verify>
  <resume-signal>Type "approved" if the in-platform workflow is correct, or describe the failure point</resume-signal>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit`
2. Prompt Opinion MCP connection health check passes
3. Prompt Opinion agent shows all three tools attached
4. Published Marketplace listing is visible
5. End-to-end in-platform test passes for coverage, criteria, and readiness questions on at least one demo patient
</verification>

<success_criteria>
- Requirements DEP-02, DEP-03, DEP-04, and DEP-05 are directly addressed
- Any SHARP context mismatch is resolved in code and documented
- The solution is usable and discoverable inside Prompt Opinion, not merely deployed outside it
- End-to-end demo workflow works with grounded, policy-cited responses
</success_criteria>

<output>
After completion, create `.planning/phases/05-deployment-integration/05-02-SUMMARY.md`
</output>
