---
phase: 04-deployment-integration
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - docs/prompt-opinion/deployment-runbook.md
  - docs/prompt-opinion/workspace-integration.md
autonomous: false

must_haves:
  truths:
    - "Deployment runbook documents all 7 tools in expected smoke test output"
    - "Deployment runbook includes deploy:ngrok as the primary deployment command"
    - "Workspace integration doc lists all 7 tools for agent configuration"
    - "Workspace integration doc includes demo scenarios for bevacizumab comparison and rituximab Q&A"
    - "User verifies MCP server is accessible via ngrok and Prompt Opinion discovers all 7 tools"
  artifacts:
    - path: "docs/prompt-opinion/deployment-runbook.md"
      provides: "Updated deployment runbook with all 7 tools and ngrok automation"
      contains: "ask_policy_question"
    - path: "docs/prompt-opinion/workspace-integration.md"
      provides: "Updated workspace integration with all 7 tools and demo scenarios"
      contains: "compare_drug_across_payers"
  key_links:
    - from: "docs/prompt-opinion/deployment-runbook.md"
      to: "scripts/deploy-ngrok.ts"
      via: "references npm run deploy:ngrok"
      pattern: "deploy:ngrok"
    - from: "docs/prompt-opinion/workspace-integration.md"
      to: "src/mcp/index.ts"
      via: "lists all registered tools"
      pattern: "list_policies"
---

<objective>
Update deployment and integration docs for all 7 tools, add demo validation scenarios, and verify end-to-end deployment via ngrok with Prompt Opinion.

Purpose: The deployment runbook and workspace integration docs reference only the original 3 tools. Phase 1-3 added 4 more tools that are the core deliverables of this hackathon. Docs must reflect the full 7-tool set, the automated deployment script, and include demo scenarios for bevacizumab comparison and rituximab Q&A. A human verification checkpoint confirms the deployment works end-to-end.

Output: Updated deployment runbook, updated workspace integration doc, verified deployment.
</objective>

<execution_context>
@/Users/hero4440/.claude/get-shit-done/workflows/execute-plan.md
@/Users/hero4440/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-deployment-integration/04-01-SUMMARY.md
@docs/prompt-opinion/deployment-runbook.md
@docs/prompt-opinion/workspace-integration.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update deployment runbook and workspace integration docs for all 7 tools</name>
  <files>docs/prompt-opinion/deployment-runbook.md, docs/prompt-opinion/workspace-integration.md</files>
  <action>
**deployment-runbook.md updates:**

1. In section 2 "Verify the local server", update expected outcome to list all 7 tools:
   - `tools/list` shows `get_drug_coverage`, `get_prior_auth_criteria`, `check_patient_readiness`, `list_policies`, `get_policy_summary`, `compare_drug_across_payers`, `ask_policy_question`

2. In section 3 "Start the HTTPS tunnel", add alternative approach using the automated script:
   ```bash
   # Automated (recommended):
   NGROK_AUTHTOKEN=your_token LOCAL_LLM_MODEL=llama3.1 npm run deploy:ngrok

   # Manual (if you prefer separate terminals):
   ngrok http 3000
   # Then in another terminal:
   PUBLIC_BASE_URL=https://abc123.ngrok-free.app LOCAL_LLM_MODEL=llama3.1 npm start
   ```

3. Add a new section before Troubleshooting: "6. Demo Validation" with these scenarios:
   - **Bevacizumab comparison:** Call `compare_drug_across_payers` with `{ "drug_family": "bevacizumab" }`. Expected: shows BCBS NC preferred/non-preferred product split.
   - **Rituximab Q&A:** Call `ask_policy_question` with `{ "question": "What prior authorization criteria does Cigna require for rituximab?" }`. Expected: returns grounded answer with evidence snippets from Cigna policy.
   - **Policy listing:** Call `list_policies` with `{}`. Expected: returns 4+ policies with payer and drug metadata.

4. In Troubleshooting, add a section for "ask_policy_question cannot reach the local model":
   - Cause: local Ollama service is unavailable or no installed model matches `LOCAL_LLM_MODEL` / `OLLAMA_MODEL`
   - Fix: Start Ollama and point the env var at an installed model before starting the server

5. Update Demo-Day Checklist to include:
   - Local Ollama is running for LLM-backed answers
   - All 7 tools visible in Prompt Opinion
   - Bevacizumab comparison returns preferred/non-preferred split
   - Rituximab Q&A returns evidence-backed answer

**workspace-integration.md updates:**

1. Update "Agent configuration path" section to list all 7 tools:
   - `get_drug_coverage`
   - `get_prior_auth_criteria`
   - `check_patient_readiness`
   - `list_policies`
   - `get_policy_summary`
   - `compare_drug_across_payers`
   - `ask_policy_question`

2. Update the recommended agent framing to mention policy comparison and Q&A capabilities:
   ```text
   You are PolicyPilot, a medical policy analysis assistant. Use the attached MCP tools to answer drug coverage, prior authorization, cross-payer comparison, and natural language policy questions. Always ground responses in policy evidence. When comparing policies across payers, highlight key differences in preferred products, step therapy requirements, and prior auth criteria.
   ```

3. Update "In-platform validation flow" to replace the 3 old demo scenarios with 5 scenarios covering all tool categories:
   1. **Policy listing** — Ask: "What policies are loaded?" Expected: uses `list_policies`
   2. **Policy summary** — Ask: "Summarize the BCBS NC bevacizumab policy" Expected: uses `get_policy_summary` with evidence
   3. **Cross-payer comparison** — Ask: "Compare bevacizumab coverage across payers" Expected: uses `compare_drug_across_payers`, shows preferred/non-preferred split
   4. **Natural language Q&A** — Ask: "What prior authorization criteria does Cigna require for rituximab?" Expected: uses `ask_policy_question`, returns grounded answer
   5. **Coverage check** — Ask: "Is bevacizumab covered under BCBS NC?" Expected: uses `get_drug_coverage` with evidence

4. Update "Completion checklist" to include all 7 tools, bevacizumab comparison, and rituximab Q&A validation.
  </action>
  <verify>Read both updated files and confirm: (a) all 7 tools are listed, (b) deploy:ngrok is referenced, (c) demo scenarios for bevacizumab and rituximab are present, (d) the local Ollama requirement is documented.</verify>
  <done>Deployment runbook and workspace integration docs reflect all 7 tools, automated deployment, demo scenarios, and environment requirements.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify ngrok deployment and Prompt Opinion integration</name>
  <what-built>
Complete deployment pipeline: MCP server with 7 tools, ngrok tunnel automation, updated smoke tests, deployment docs, and Prompt Opinion integration guides.
  </what-built>
  <how-to-verify>
1. **Start the deployment:**
   ```bash
   NGROK_AUTHTOKEN=your_token LOCAL_LLM_MODEL=llama3.1 npm run deploy:ngrok
   ```
   Verify: tunnel URL printed to stderr, server starts on port 3000

2. **Run remote smoke test:**
   ```bash
   BASE_URL=https://your-ngrok-url.ngrok-free.app npm run smoke:mcp
   ```
   Verify: all 3 checks pass (health, 7 tools discovered, list_policies call succeeds)

3. **Test in Prompt Opinion:**
   - Add MCP connection with your ngrok `/mcp` URL
   - Verify all 7 tools are discovered
   - Ask: "Compare bevacizumab coverage across payers" — verify BCBS NC preferred/non-preferred split appears
   - Ask: "What prior authorization criteria does Cigna require for rituximab?" — verify evidence-backed answer

4. **Verify health endpoint:**
   ```bash
   curl https://your-ngrok-url.ngrok-free.app/health
   ```
   Verify: returns status ok with correct policy/payer/drug counts
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe what failed.</resume-signal>
</task>

</tasks>

<verification>
1. Deployment runbook lists all 7 tools and references `deploy:ngrok`
2. Workspace integration lists all 7 tools in agent configuration
3. Demo scenarios for bevacizumab and rituximab are documented
4. Local Ollama requirement is documented
5. Human verified: ngrok deployment works, Prompt Opinion discovers all 7 tools, demo scenarios execute successfully
</verification>

<success_criteria>
MCP server deployed via ngrok with all 7 tools discoverable and callable from Prompt Opinion. Bevacizumab comparison shows BCBS NC preferred/non-preferred split. Rituximab Q&A returns evidence-grounded answer. Deployment documented for reproducible hackathon demos.
</success_criteria>

<output>
After completion, create `.planning/phases/04-deployment-integration/04-02-SUMMARY.md`
</output>
