---
phase: 07-policy-compare-insights
plan: "03"
type: execute
wave: 2
depends_on:
  - "07-01"
  - "07-02"
files_modified:
  - src/server/policy-compare.ts
  - src/frontend/pages/PolicyComparePage.tsx
  - src/frontend/styles.css
autonomous: true
requirements:
  - COMP-03

must_haves:
  truths:
    - "Meaningful cross-payer differences are called out above the compare table"
    - "Difference generation is deterministic and explainable from normalized comparison rows"
    - "Highlights suppress noise when all selected payers are materially the same"
  artifacts:
    - path: "src/server/policy-compare.ts"
      provides: "Deterministic difference highlight generation"
      exports:
        - buildComparisonHighlights
    - path: "src/frontend/pages/PolicyComparePage.tsx"
      provides: "Highlight cards rendered above the compare matrix"
      exports:
        - PolicyComparePage
  key_links:
    - from: "src/server/policy-compare.ts"
      to: "src/frontend/pages/PolicyComparePage.tsx"
      via: "compare response highlight array"
      pattern: "highlights"
---

<objective>
Add analyst-grade difference highlights to the compare experience.

Purpose: The compare matrix alone is readable, but Phase 7 explicitly requires fast difference detection. This plan adds deterministic highlight logic that surfaces material differences without hand-written copy.
Output: highlight generator in the compare domain and highlight cards in the compare page UI.
</objective>

<execution_context>
@/Users/tanmaybhuskute/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tanmaybhuskute/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/07-policy-compare-insights/07-CONTEXT.md
@.planning/phases/07-policy-compare-insights/07-01-PLAN.md
@.planning/phases/07-policy-compare-insights/07-02-PLAN.md
@src/server/policy-compare.ts
@src/frontend/pages/PolicyComparePage.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement deterministic compare highlight generation</name>
  <files>src/server/policy-compare.ts</files>
  <action>
    Extend the compare domain to generate a `highlights` array.

    The generator should:
    - compare normalized row values across selected payers
    - emit highlight categories such as:
      - stricter requirement
      - missing requirement
      - preferred vs non-preferred split
      - covered indication mismatch
      - prior auth mismatch
      - step therapy mismatch
    - ignore rows where all selected payers are materially equivalent
    - cap default output to the most meaningful highlights first

    Each highlight should reference which row(s) and payer(s) caused it so the UI can deep-link or visually anchor the difference.
  </action>
  <verify>
    Compare responses now include a stable `highlights` array.
    At least one known multi-payer comparison returns meaningful highlight text.
  </verify>
  <done>
    Deterministic highlight generation exists and can explain major payer differences.
  </done>
</task>

<task type="auto">
  <name>Task 2: Render highlight cards above the compare matrix</name>
  <files>src/frontend/pages/PolicyComparePage.tsx, src/frontend/styles.css</files>
  <action>
    Render the server-generated highlight list above the comparison table.

    Requirements:
    - highlight cards are visually distinct from the table
    - cards read as concise analyst observations, not generic badges
    - clicking a highlight can optionally focus/open the related row or evidence, if practical
    - no-highlight state is explicit: “Selected payers look materially similar for current loaded rules”
  </action>
  <verify>
    `npm run frontend:build` passes.
    Highlights appear above the table when selected payers differ.
  </verify>
  <done>
    COMP-03 is satisfied in the UI with deterministic highlight cards.
  </done>
</task>

</tasks>

<verification>
- Highlight generation is deterministic and tied to compare rows
- Noise is suppressed when values are equivalent
- Highlight cards render above the compare matrix
</verification>

<success_criteria>
- COMP-03 is implemented end-to-end
- Analysts can spot key payer differences without scanning every row first
</success_criteria>

<output>
After completion, create `.planning/phases/07-policy-compare-insights/07-03-SUMMARY.md` with:
- highlight categories implemented
- examples of generated highlight phrasing
- edge cases suppressed as noise
- verification results
</output>
