---
phase: 07-policy-compare-insights
plan: "05"
type: execute
wave: 3
depends_on:
  - "07-03"
  - "07-04"
files_modified:
  - src/server/policy-insights.ts
  - src/frontend/pages/PolicyInsightsPage.tsx
  - src/frontend/styles.css
autonomous: false
requirements:
  - INSG-03
  - INSG-04

must_haves:
  truths:
    - "Policy Insights page shows a simple knowledge graph of drug -> payer -> policy -> rule relationships"
    - "Graph nodes or edges can open the shared evidence panel when evidence is available"
    - "Phase 7 is human-verified across compare, highlights, heat map, and graph flows"
  artifacts:
    - path: "src/frontend/pages/PolicyInsightsPage.tsx"
      provides: "Combined heat map + knowledge graph insights experience"
      exports:
        - PolicyInsightsPage
  key_links:
    - from: "knowledge graph nodes/edges"
      to: "shared evidence panel"
      via: "click handler"
      pattern: "setSelectedEvidence"
---

<objective>
Finish Phase 7 with the knowledge graph view and a blocking human verification pass.

Purpose: The phase is not done until users can both compare and inspect patterns visually. This plan adds the last required visual and confirms the full phase works end-to-end.
Output: knowledge graph on the Insights page and a human-verified Phase 7 flow.
</objective>

<execution_context>
@/Users/tanmaybhuskute/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tanmaybhuskute/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/07-policy-compare-insights/07-CONTEXT.md
@.planning/phases/07-policy-compare-insights/07-03-PLAN.md
@.planning/phases/07-policy-compare-insights/07-04-PLAN.md
@src/server/policy-insights.ts
@src/frontend/pages/PolicyInsightsPage.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add simple knowledge graph to Policy Insights</name>
  <files>src/server/policy-insights.ts, src/frontend/pages/PolicyInsightsPage.tsx, src/frontend/styles.css</files>
  <action>
    Extend the insights payload and page to show a simple/static relationship graph for:

    `drug -> payer -> policy -> rule`

    Requirements:
    - do not introduce unnecessary visualization complexity
    - keep labels readable in the existing portal layout
    - clicking a node or related edge opens or focuses evidence where possible
    - if direct evidence is not applicable to the selected node, explain what the node represents
  </action>
  <verify>
    `npm run frontend:build` passes.
    The Insights page shows a readable relationship graph and interaction does not break the heat map view.
  </verify>
  <done>
    INSG-03 is satisfied with a simple graph on the Policy Insights page.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verify complete Phase 7 workflow</name>
  <files>none</files>
  <action>
    No code changes. Human tests the compare and insights workflows end-to-end using loaded policies from the v2 store.
  </action>
  <verify>
    Start the dev stack: `npm run server:dev` in one terminal, `npm run frontend:dev` in another.
    Open http://localhost:4173 and run all verification scenarios listed below.
  </verify>
  <done>All Phase 7 compare and insights scenarios pass with no blocking errors.</done>
  <how-to-verify>
    Start the dev stack: `npm run server:dev` in one terminal, `npm run frontend:dev` in another.
    Open http://localhost:4173 in your browser.

    Test 1 — Compare table (COMP-01, COMP-02):
    1. Open the Compare page
    2. Select a drug family with at least two loaded payers
    3. Select two or more payers
    4. Confirm the table shows preferred products, non-preferred products, prior auth, step therapy, covered indications, and key restrictions

    Test 2 — Difference highlights (COMP-03):
    1. Keep the same compare selection
    2. Confirm highlight cards appear above the table
    3. Verify at least one highlight accurately reflects a real payer difference

    Test 3 — Compare evidence panel (COMP-04):
    1. Click a populated comparison cell
    2. Confirm the evidence panel opens
    3. Verify snippet text, filename, and page number are shown

    Test 4 — Insights heat map (INSG-01, INSG-02, INSG-04):
    1. Open Policy Insights
    2. Change at least two filters (for example drug family + rule type)
    3. Confirm the heat map updates
    4. Click a colored heat map cell and confirm evidence opens with page reference

    Test 5 — Knowledge graph (INSG-03):
    1. On the same Policy Insights page, inspect the relationship graph
    2. Confirm the graph shows drug -> payer -> policy -> rule relationships
    3. Click a node or edge and confirm evidence or node context appears
  </how-to-verify>
  <resume-signal>Type "approved" if all 5 tests pass, or describe what failed and what you saw</resume-signal>
</task>

</tasks>

<verification>
All 5 Phase 7 scenarios pass:
1. Compare table renders required rows for selected payers
2. Difference highlights are meaningful
3. Compare evidence panel shows source snippets with page references
4. Heat map filters work and heat map cells open evidence
5. Knowledge graph renders and supports drill-in/context
</verification>

<success_criteria>
- Phase 7 requirements are complete and human-verified
- Compare and Insights both operate on the same v2 policy data model
- Evidence-first interaction is consistent across matrix, heat map, and graph
</success_criteria>

<output>
After completion, create `.planning/phases/07-policy-compare-insights/07-05-SUMMARY.md` with:
- knowledge graph implementation summary
- end-to-end UAT results
- any remaining polish gaps that do not block Phase 8
- final Phase 7 readiness statement
</output>
