---
phase: 07-policy-compare-insights
plan: "02"
type: execute
wave: 1
depends_on:
  - "07-01"
files_modified:
  - src/frontend/pages/PolicyComparePage.tsx
  - src/frontend/components/PolicyEvidencePanel.tsx
  - src/frontend/App.tsx
  - src/frontend/styles.css
autonomous: true
requirements:
  - COMP-01
  - COMP-02
  - COMP-04

must_haves:
  truths:
    - "Users can select a drug family and two or more payers from the portal UI"
    - "The compare page renders a side-by-side table with one payer column per selected payer"
    - "Each required row is visible: preferred products, non-preferred products, prior auth, step therapy, covered indications, key restrictions"
    - "Clicking a comparison cell opens an evidence panel with snippet and page number"
  artifacts:
    - path: "src/frontend/pages/PolicyComparePage.tsx"
      provides: "Phase 7 compare page container using the new compare API"
      exports:
        - PolicyComparePage
    - path: "src/frontend/components/PolicyEvidencePanel.tsx"
      provides: "Shared evidence drawer/panel for compare and insights"
      exports:
        - PolicyEvidencePanel
  key_links:
    - from: "src/frontend/pages/PolicyComparePage.tsx"
      to: "src/frontend/data/policies.ts"
      via: "fetchPolicyComparison"
      pattern: "fetchPolicyComparison"
    - from: "comparison table cell"
      to: "src/frontend/components/PolicyEvidencePanel.tsx"
      via: "click handler -> selected evidence state"
      pattern: "setSelectedEvidence"
---

<objective>
Build the user-facing compare page and evidence interaction model.

Purpose: Phase 7 needs a real analyst workflow, not just a backend payload. This plan makes compare usable in the portal and enforces evidence-first interaction at the cell level.
Output: compare page, shared evidence panel, App navigation wiring, styling for the new compare matrix.
</objective>

<execution_context>
@/Users/tanmaybhuskute/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tanmaybhuskute/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-policy-compare-insights/07-CONTEXT.md
@.planning/phases/07-policy-compare-insights/07-01-PLAN.md
@src/frontend/App.tsx
@src/frontend/components/compare-builder-view.tsx
@src/frontend/components/compare-view.tsx
@src/frontend/styles.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create compare page container and table UI</name>
  <files>src/frontend/pages/PolicyComparePage.tsx, src/frontend/App.tsx, src/frontend/styles.css</files>
  <action>
    Create `PolicyComparePage` as the v2 compare experience.

    Requirements:
    - drug family selector
    - payer multi-select / checklist
    - compare action state that requires at least two payers
    - comparison matrix with one column per selected payer
    - required rows from COMP-02
    - clear empty/loading/error states

    Update `App.tsx` so Compare points to the new page rather than the legacy antonrx compare view. Reuse the current workspace shell structure where possible, but the page should clearly read as a Phase 7 policy compare workflow.
  </action>
  <verify>
    `npm run frontend:build` passes.
    User can load the page, select at least two payers, and see the compare matrix render.
  </verify>
  <done>
    The portal has a working v2 compare page that renders required rows across selected payer columns.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add shared evidence panel interaction</name>
  <files>src/frontend/components/PolicyEvidencePanel.tsx, src/frontend/pages/PolicyComparePage.tsx, src/frontend/styles.css</files>
  <action>
    Create a reusable evidence panel component and wire comparison cells to it.

    Requirements:
    - compare cells with evidence are visibly clickable
    - clicking a cell opens evidence references for that row/payer
    - each evidence reference shows snippet text, filename, and page number
    - if no evidence exists, the UI explains that the value is currently derived without a direct snippet

    Keep the evidence panel reusable for the later Policy Insights page.
  </action>
  <verify>
    `npm run frontend:build` passes.
    Clicking a compare cell opens an evidence panel with page-level references.
  </verify>
  <done>
    COMP-04 is satisfied in the compare UI via a shared evidence panel.
  </done>
</task>

</tasks>

<verification>
- Compare page renders with live API data
- Required compare rows are visible
- Evidence panel opens from comparison cells
- App navigation reaches the new compare workflow cleanly
</verification>

<success_criteria>
- Users can complete the core compare flow from the portal
- COMP-01, COMP-02, and COMP-04 are visible and testable in the UI
- Shared evidence panel component exists for reuse in the insights page
</success_criteria>

<output>
After completion, create `.planning/phases/07-policy-compare-insights/07-02-SUMMARY.md` with:
- compare page UX summary
- evidence panel behavior
- notable responsive/layout decisions
- verification results
</output>
