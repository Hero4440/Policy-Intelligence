---
phase: 07-policy-compare-insights
plan: "04"
type: execute
wave: 2
depends_on:
  - "07-01"
  - "07-02"
files_modified:
  - src/server/policy-insights.ts
  - src/server/policy-routes.ts
  - src/frontend/pages/PolicyInsightsPage.tsx
  - src/frontend/components/PolicyEvidencePanel.tsx
  - src/frontend/App.tsx
  - src/frontend/styles.css
autonomous: true
requirements:
  - INSG-01
  - INSG-02
  - INSG-04

must_haves:
  truths:
    - "Policy Insights page shows a heat map of payer × rule type × coverage status"
    - "Users can filter the heat map by drug family, payer, rule type, and version"
    - "Clicking a heat map cell opens the shared evidence panel with snippet and page reference"
  artifacts:
    - path: "src/server/policy-insights.ts"
      provides: "Insights aggregation and filterable heat map payloads"
      exports:
        - buildPolicyInsights
    - path: "src/frontend/pages/PolicyInsightsPage.tsx"
      provides: "Policy Insights page with filters and heat map"
      exports:
        - PolicyInsightsPage
  key_links:
    - from: "src/server/policy-routes.ts"
      to: "src/server/policy-insights.ts"
      via: "GET /api/policies/insights"
      pattern: "buildPolicyInsights"
    - from: "heat map cell"
      to: "src/frontend/components/PolicyEvidencePanel.tsx"
      via: "cell click -> selected insight evidence"
      pattern: "setSelectedEvidence"
---

<objective>
Implement the Policy Insights page with a filterable heat map and evidence-backed drill-in.

Purpose: Compare helps with side-by-side analysis; Insights helps users spot patterns across the loaded corpus. This plan adds the filterable heat map view and evidence panel interaction required by Phase 7.
Output: insights aggregation service, insights API route, Policy Insights page, shared evidence panel reuse.
</objective>

<execution_context>
@/Users/tanmaybhuskute/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tanmaybhuskute/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/07-policy-compare-insights/07-CONTEXT.md
@.planning/phases/07-policy-compare-insights/07-01-PLAN.md
@src/server/policy-compare.ts
@src/server/policy-routes.ts
@src/frontend/App.tsx
@src/frontend/styles.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build insights aggregation and filterable endpoint</name>
  <files>src/server/policy-insights.ts, src/server/policy-routes.ts</files>
  <action>
    Create `src/server/policy-insights.ts` that aggregates stored policies into a heat-map-friendly shape.

    Required dimensions:
    - payer
    - rule type
    - coverage status / posture
    - drug family
    - version

    Add an endpoint such as:
    - `GET /api/policies/insights?...filters`

    Requirements:
    - support filtering by drug family, payer, rule type, and version
    - emit stable semantic statuses used by the UI color mapping
    - include evidence references per cell so clicking a cell can open source snippets
  </action>
  <verify>
    Route returns a heat map payload with filter dimensions and evidence metadata.
  </verify>
  <done>
    The server can build filterable insights payloads for the Policy Insights page.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build Policy Insights page and heat map UI</name>
  <files>src/frontend/pages/PolicyInsightsPage.tsx, src/frontend/components/PolicyEvidencePanel.tsx, src/frontend/App.tsx, src/frontend/styles.css</files>
  <action>
    Create a new Policy Insights page and expose it through top-level nav.

    Requirements:
    - filters for drug family, payer, rule type, and version
    - heat map grid/table with semantic colors
    - legend explaining the color mapping
    - clicking a cell opens the shared evidence panel
    - usable empty states when filter combinations return no cells

    Keep the page visually consistent with the workspace shell, but treat Insights as its own top-level workflow rather than overloading the existing detail tabs.
  </action>
  <verify>
    `npm run frontend:build` passes.
    Filters update the heat map and cell clicks open evidence with page reference.
  </verify>
  <done>
    INSG-01, INSG-02, and the heat-map side of INSG-04 are implemented end-to-end.
  </done>
</task>

</tasks>

<verification>
- Heat map payload is filterable and evidence-backed
- Policy Insights page renders with filters and legend
- Clicking a heat map cell opens the shared evidence panel
</verification>

<success_criteria>
- Users can spot restrictive/favorable patterns across policies, not just single compares
- INSG-01, INSG-02, and part of INSG-04 are satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/07-policy-compare-insights/07-04-SUMMARY.md` with:
- heat map dimensions and status mapping
- filter behavior
- evidence panel reuse notes
- verification results
</output>
