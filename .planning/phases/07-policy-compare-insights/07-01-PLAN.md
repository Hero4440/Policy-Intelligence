---
phase: 07-policy-compare-insights
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/server/policy-compare.ts
  - src/server/policy-routes.ts
  - src/server/index.ts
  - src/frontend/data/policies.ts
autonomous: true
requirements:
  - COMP-01
  - COMP-02
  - COMP-04

must_haves:
  truths:
    - "The compare flow reads from the v2 policy store, not the legacy antonrx dataset"
    - "Given a drug family and two or more payers, the server returns one normalized comparison column per payer"
    - "Each comparison row includes display-ready values plus source evidence metadata"
    - "Frontend data clients can request compare payloads without importing hardcoded JSON policy files"
  artifacts:
    - path: "src/server/policy-compare.ts"
      provides: "Normalization and evidence-backed compare payload builders for stored policies"
      exports:
        - buildPolicyComparison
        - listComparableDrugFamilies
    - path: "src/server/policy-routes.ts"
      provides: "Policy compare endpoints for the v2 portal"
      exports:
        - registerPolicyRoutes
  key_links:
    - from: "src/server/policy-routes.ts"
      to: "src/server/policy-compare.ts"
      via: "GET /api/policies/compare"
      pattern: "buildPolicyComparison"
    - from: "src/frontend/data/policies.ts"
      to: "src/server/policy-routes.ts"
      via: "fetch compare payload"
      pattern: "/api/policies/compare"
---

<objective>
Create the Phase 7 comparison domain/API foundation on top of the file-backed policy store.

Purpose: Phase 7 cannot be credible if compare still runs on the legacy demo dataset while policy management writes to the v2 store. This plan makes the v2 store the source of truth for compare.
Output: compare normalization module, compare/list endpoints, frontend data client updates.
</objective>

<execution_context>
@/Users/tanmaybhuskute/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tanmaybhuskute/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-policy-compare-insights/07-CONTEXT.md
@src/storage/policy-store.ts
@src/storage/startup.ts
@src/server/index.ts
@src/frontend/data/policies.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build v2 compare normalization service</name>
  <files>src/server/policy-compare.ts</files>
  <action>
    Create `src/server/policy-compare.ts` as the new Phase 7 domain module. It should:

    1. Read policies and versions from the v2 storage modules rather than `antonrx`
    2. Export stable compare types for:
       - compare filters
       - compare rows
       - compare evidence references
       - payer columns / policy version metadata
    3. Normalize each policy into these required rows:
       - preferred products
       - non-preferred products
       - prior auth
       - step therapy
       - covered indications
       - key restrictions
    4. For each row, return:
       - `value`
       - `status`
       - `evidence[]` with snippet, filename, page, section/field label
    5. Export `listComparableDrugFamilies()` for sidebar/filter population

    Use deterministic transforms only. Do not introduce LLM summarization here.
  </action>
  <verify>
    `npm run frontend:build` still passes.
    A local one-shot script or route call can return compare payloads for at least one known stored drug family.
  </verify>
  <done>
    A standalone server module exists that produces evidence-backed compare payloads from the v2 policy store.
  </done>
</task>

<task type="auto">
  <name>Task 2: Expose compare endpoints and frontend client</name>
  <files>src/server/policy-routes.ts, src/server/index.ts, src/frontend/data/policies.ts</files>
  <action>
    Add or extend v2 policy routes with endpoints for:

    - `GET /api/policies/compare?drugFamily=...&payers=...`
    - `GET /api/policies/compare/options`

    Requirements:
    - Validate that at least two payers are selected for compare
    - Return current policy version by default
    - Surface 4xx errors for invalid input instead of silent empty payloads

    Update `src/frontend/data/policies.ts` to add fetch helpers for these endpoints and define the shared TypeScript response types used by the UI.

    Wire route registration from `src/server/index.ts`.
  </action>
  <verify>
    `npm run frontend:build` passes.
    Hitting the new endpoints returns JSON with normalized rows and evidence metadata.
  </verify>
  <done>
    Compare endpoints are live and the frontend has a typed data client for Phase 7 compare flows.
  </done>
</task>

</tasks>

<verification>
- Compare payloads come from the v2 policy store
- Required comparison rows are present
- Evidence metadata is included for row values
- Frontend build passes without importing legacy hardcoded policy JSON for compare data
</verification>

<success_criteria>
- Phase 7 has a real domain/API foundation tied to current policy versions
- COMP-01, COMP-02, and the data side of COMP-04 are unblocked
- No new compare dependency is introduced on the legacy antonrx path
</success_criteria>

<output>
After completion, create `.planning/phases/07-policy-compare-insights/07-01-SUMMARY.md` with:
- final compare endpoint shapes
- normalization rules per comparison row
- any policy fields that required fallback heuristics
- verification results
</output>
